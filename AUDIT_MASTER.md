# FinFlow — Master Audit (merged)

**Date:** 9 July 2026
**Sources merged:** the code-agent audit (`AUDIT_CODE.md`, 20 findings, runtime-verified) + the chat audit (`CODE_AUDIT_2026-07-09.md`), deduplicated into one list. Where the two overlapped, the runtime-verified version is kept. Three chat-only items are added (M-A/M-B/M-C). One chat finding was **withdrawn**: the "stale bundle" Critical — the code agent re-minified both sources with the repo's own `terser@5.48.0` and got the committed `.min.js` byte-for-byte, so the bundles are in sync. (The residual risk lives on as F13: nothing *enforces* that sync.)

**Verified by running code** (not guessed): bundle byte-match, Express error-handler ordering, partial-PUT JSONB corruption, `/register?ref=` fallthrough, and the auth-boundary 401s.

---

## Master findings

| # | Sev | Finding | file:line | Fix | Source |
|---|-----|---------|-----------|-----|--------|
| F1 | ✅ **FIXED** (was 🔴 Critical) | Any verified accountant can read **any** user's books — `link-client` writes `accountant_id` onto an arbitrary user with no consent; `/books` trusts it via a `UNION` branch with no status check | `accountant-routes.js:638,441` | **Done:** deleted `link-client` (sole JSONB writer, UI-unreachable) + orphaned `referralMonthsForTier`; replaced all 8 JSONB `(data->>'accountant_id')::int=$1` access branches + 4 status-less gates (notes/flag/checklist) with `accountant_clients.status='active'`; removed admin `jsonbClients` listing branch. Live-DB test: breach closed, legit active path preserved. **Deferred hygiene:** stale `users.data.accountant_id` values are now inert (no reader) — optional one-time `UPDATE users SET data = data - 'accountant_id'`. | both (agent escalated) |
| F2 | ✅ **FIXED** (was 🔴 Critical) | Partial `PUT` nulls `amount` and deletes fields on recurring bills/invoices (whole-object patch with `undefined` keys); reproduced corrupt JSONB | `server.js:1859,1931`; `database.js:674` | **Done:** recurring bills/invoices PUT rebuilt with conditional patches (`if (x!=null) patch.x=…`), mirroring the invoices/expenses PUT idiom. **Also fixed the same corruption class in `payments_made` PUT (server.js:2096)** — its `|| default` coalescing reset missing fields (amount→0, date→today, vendor/method/notes/ref→''). Both reproduced (partial PUT wiped/clobbered siblings) and verified fixed via live-DB test. Swept all 50 `updateById`/`update` sites: holdings PUT and all other money PUTs already used conditional patches (safe). | agent |
| F3 | ✅ **FIXED** (was 🟠 High) | `unrealised_gain_loss` displayed/summed but **never computed** (only `realised` is written) — the column is `DEFAULT 0` and no code path ever writes it, so open-position P/L always showed +$0.00 | `server.js:2765,3413`; `index.html:4497,4525`; `database.js:346` | **Done — compute at read time.** Inputs all existed (`rate_at_transaction`, `foreign_amount` stored; current rate from the feature's own `fx_rates` table — NOT the frontend's static `_safeFX`). Added `latestFxRates` + `computeUnrealised`; `/api/fx-transactions`, `/api/fx-summary`, `/api/reports` now compute `(currentRate − rateAtTxn) × foreignAmount` per open position instead of reading the dead column. **No current rate for a pair → `null`, never a fabricated $0** (UI shows "—"; summary excludes it and reports `openWithoutRate`). Also fixed **BUG A** — `loadFXData` read `summary.unrealised/realised` but the endpoint returns `totalUnrealised/totalRealised/netFX`, which had zeroed **all three** cards incl. the correctly-computed realised value. Live-DB 12/12 (open EUR 10k @1.10, rate 1.20 → +1000; open w/o rate → null + flagged; realised −150 preserved; netFX 850). Live UI 8/8. | both |
| F4 | ✅ **FIXED** (was 🟠 High) | Global error handler sits **before ~42 routes** → those return HTML/stack instead of JSON (stack leak in non-prod) | `server.js:2521` (routes 2617–3560) | **Done:** relocated the global error handler to the very bottom (after all routes + the `/api` 404 + the `*` SPA fallback), a pure move with handler logic unchanged. The `/api` 404 + `*` fallbacks were already at the bottom — only the handler was mis-placed. Verified on the real app: handler is the last router layer (after `/api` 404 → `*`); live tests confirm affected-route errors → JSON 500, `/api` 404 → JSON, SPA/landing still serve. | agent |
| F5 | ✅ **FIXED** (was 🟠 High) | RBAC account-resolver was inert — `team_members.member_user_id` written **nowhere**, so `req.accountId` always == own id. **Step A (done):** the invite/accept spine now writes real membership rows (`member_user_id` + `status:'active'`) via a sha256-hashed, single-use, 7-day token — `POST /api/team/invite` → `GET`/`POST /api/team/accept` → the resolver links member→owner (role from the invite row, no self-escalation). Functional index `idx_team_members_member_user_id` added. Verified end-to-end (29/29) incl. double-accept, expired, revoked, email-collision, escalation. **Step B (done):** accountant convergence — a client invites their accountant via the same machinery (`role:'accountant'`); on accept the accountant becomes a real `users` identity linked through the `accountants.user_id` bridge (set-once) and resolves into the client account, not the legacy `accountant_clients`-only path (which keeps running during migration). Verified 19/19. **Step 4 (done — per-route enforcement):** found + fixed the live bypass — the coarse gate read `req.session.userRole` (the actor's OWN account role, always `owner` for a normal signup) instead of the resolved `req.accountRole`, so every invited member was judged as owner and the gate did nothing. New single-source `rbac.js` (role→capability MATRIX + `roleHasPerm` + `requirePerm(cap)`; owner implicit-all, fail-closed). Coarse gate re-keyed to `req.accountRole` as the method-based catch-all (viewer read-only, DELETE=owner/admin) + a `READ_ONLY_POST` exemption so viewers reach the POST-but-read-only report routes; `requirePerm` wired on 20 sensitive routes for the finer limits a method rule can't express — entities/bank/permissions (owner-only), payroll/team/settings/**audit** (owner+admin; external accountant excluded from the full activity trail), AI chat (viewer excluded). The stale `/api/permissions` matrix UI (never enforced) relabeled **read-only "role defaults"** and its one mismatched cell fixed (audit·accountant→✗). Verified 73/73 over real HTTP (real `requirePerm` + coarse gate): full per-role matrix, own-account solo user unchanged, 401 unauth, and the **freshness** guarantee — owner revokes a member → their very next request (no re-login) no longer resolves into the owner's account. | `server.js:480,489,572,676` + `/api/team/invite`,`/api/team/accept`; `rbac.js`; `database.js` initDB; `public/index.html` | ~~Populate `member_user_id`/`status`~~ ✅ ~~wire `requirePerm`~~ ✅ all done | both (agent proved mechanism) |
| F6 | ✅ **FIXED** (was 🟠 High) | Weighted-average COGS mis-costs fractional units (`Math.max(units,1)`) and returns 0 cost for un-purchased sales → gross profit overstated; also coexists with FIFO elsewhere | `server.js:2755,3284` (FIFO at `3205`) | **Done — standardized on FIFO everywhere.** Chose FIFO (the app already computed + persisted it at point of sale; more defensible small-biz default). Deleted both weighted-avg aggregates (`/api/reports`, `/api/cogs`) — the `Math.max(units,1)` formula is gone entirely, so the fractional-unit bug can't recur. Added a shared FIFO core (`fifoConsume`) used by both the point-of-sale path and the new aggregate (`fifoItemTotal`); refactored `calculateFIFOCOGS` onto it (point-of-sale output byte-for-byte unchanged, proven). No-cost-basis is now **flagged, never silently costed $0**: uncovered units surface per-item + a dashboard banner. Also fixed the `/api/cogs` camelCase/snake_case key mismatch that had the Total-COGS & Gross-Profit KPIs stuck at $0. Live-DB: 12/12 (fractional→50 not 25; no-history→0 cogs + 5 uncovered flagged; two-price aggregate→68 not weighted-avg 84; aggregate reconciles with Σ point-of-sale; point-of-sale unchanged). Live UI: 8/8. | both |
| F7 | ✅ **FIXED** (was 🟠 High) | Same dashboard KPI (`d-exp`/`d-profit`) written by 2+ disagreeing formulas — one includes payroll, one doesn't; a third basis exists server-side. The bundle **patched `updateDashboard` to overwrite** app-main's canonical writer → last-writer-wins flicker ($18K/$67K/$25K for the same data) | `app-main.js:1828` vs `finflow-api-wiring-dashboard.js:190`; `server.js:2724` | **Done (Step 4):** app-main `updateDashboard` (canonical R1/E1 − FIFO COGS) is now the **SOLE writer** of `d-rev`/`d-exp`/`d-profit`. Neutralized the wiring's competing writers — `updateKPIs` stops writing the three (keeps its other cards), `_forceKPIs` replaced by a canonical `updateDashboard()` call, `_refreshDashboardUI`'s payroll patch removed. COGS pulled into the frontend as `window._cogsTotal` (entity-scoped `/api/cogs`, stashed at boot + by `loadCOGS`) and subtracted in dashboard/AI/health so headline = Net = Revenue − COGS − OpEx. `/api/reports` migrated to `computeBooks` so it reconciles too. Cash Flow/river keep the cash-basis `PROFIT[]` (never the accrual path). Bundles regenerated. No-override-drift verified (bundle has no `d-profit` value-writer); jsdom flicker test: `d-profit` stable across boot/toggle/edit; full-stack reconciliation dashboard == AI == health == `/api/reports` == `/books`. 12/12. | both |
| F8 | ✅ **RESOLVED BY REMOVAL** (was 🟠 High) | Stale/incorrect payroll bracket constants (e.g. CA federal top base `31016`→`31057.56`; ON bases off); all 2024, undated, drift yearly | `server.js:3134,3153` | **Done — engine deleted, not fixed:** removed `calculatePayroll` (all jurisdictions/brackets), `GET /api/payroll/preview`, the Tax Preview Calculator, all jurisdiction selectors + `autoSetPayrollJurisdiction`. FinFlow now performs NO tax calculation and knows no country's tax rules. Replaced the flat `tax_rate` with **user-defined deduction rows** `{label,value,type:'percent'\|'fixed'}`; `net = gross − Σ(rows)`. Added a disclaimer near every deductions input + on payroll output. Two-engine (app-main + medium.js override) updated, all 3 bundles regenerated. Live-DB: 15/15 (mixed %/fixed, Trinidad PAYE+NIS→4311, run persistence, owner-salary sync net in native currency + recurring link). Live UI (real min artifacts): 16/16. | both (agent pinned numbers) |
| F9 | ✅ **FIXED** (was 🟠 High) | Accountant `/books` read `WHERE user_id=$1` with **no entity scoping** and counted **unpaid invoices as income** (R11) — totals never reconciled with the client's entity-scoped dashboard | `accountant-routes.js:453` | **Done (Steps 2 + 2b):** `/books` now uses canonical `computeBooks(userId, entityId, period)` — the SAME function the client dashboard uses — with `?entity_id=` + `?period=` (month/quarter/year; 'all'→year). Income is paid-only + receipts + payments; OpEx incl. payroll accrual; Net incl. FIFO COGS. `computeBooks` extended to be period-aware (mirrors R1/E1 per period, re-verified). The accountant portal (`accountant-client.html`) rewired to **consume** the canonical summary (dashboard/P&L/tax/cash-flow/balance-sheet) and re-fetch per entity+period; portal "year" tab now = all-records to match the client (F25). Bonus: portal payroll repointed off the F8-deleted `tax_rate` to the deduction model. On-screen DISPLAY reconciliation verified in jsdom (13/13); server per-period 21/21; route-level 17/17. (The `accountant-dashboard.html` books modal is dead code — see F27.) | both |
| F10 | ✅ **FIXED** (was 🟠 High) | Accountant invite funnel was dead — no `/register` route, so `/register?ref=` fell to the catch-all (`landing.html`) and dropped `ref`; signup never forwarded the code | `accountant-routes.js:602`; `server.js:3569` | **Done (Step C):** added `/register` → `302 /app?signup=1&ref=CODE` (before the catch-all; forwards a valid `plan`, caps ref at 50 chars); `doRegister()` now reads `ref` from the URL and posts it as `referralCode` (mirrors the existing `plan` read). Backend capture (`server.js:301`) already linked the signup to the verified accountant. Verified 12/12 (redirect logic + pg-mem backend capture: valid→pending link, unknown/unverified/empty→no-op, duplicate idempotent). | agent |
| F11 | ✅ **FIXED** (was 🟠 High) | Referral payout cron filtered on `subscriptionStatus` which was **never written**, so it paid nobody; webhook never called activate/suspend | `accountant-routes.js:805`; `server.js:100` | **Done (Step D):** webhook now writes `subscriptionStatus` on `customer.subscription.created/updated/deleted` + checkout completion, and calls shared `suspendClientForUser`/`reactivateClientForUser`; payout cron tightened to `subscriptionStatus='active'` (paying-only, trial excluded). Verified 21/21 (pg-mem). **Stripe-gated live checks in the Railway section.** | agent (corrects chat's "double-pay") |
| F12 | ✅ **FIXED** (was 🟡 Medium) | Admin panel surfaced never-written fields (`subscriptionStatus`, `trialEnds` — code writes `trial_ends`) as if real | `admin-routes.js:446` | **Done (Step E):** the client-detail query now reads `data->>'trial_ends'` (the key actually written), so the trial column is no longer permanently blank; `subscriptionStatus` is now a genuinely-populated field (Step D's webhook writes it on `customer.subscription.*` / checkout — no longer phantom). The admin surface now also has a real **pending-accountant review UI** (approve/reject modal + the Step-F credential viewer), closing the "no way to review" gap. **RESEND-gated:** trial values only populate once trials are actually written; sub-status only once Stripe events land (Railway section). | agent |
| F13 | ✅ **FIXED** (was 🟡 Medium) | No build step; bundle sync was manual/unenforced — edit a wiring source, forget `node bundle.js`, and the served (generated) `finflow-bundle.js` silently keeps the old code (root of the two-engine hazard). **Investigation reframed two premises:** the served files are the **non-minified** `app-main.js` + `finflow-bundle.js` (index.html loads `.js`, not `.min.js`), so (a) the `.min.js` files were **dead** (served nowhere; only `audit.js` referenced them) and (b) **terser was never needed** — `bundle.js` is pure concatenation, so `--production` dropping terser was a non-issue. `app-main.js` is hand-edited AND served (can't drift); only the generated `finflow-bundle.js` needed a sync guard. | `package.json`; `bundle.js`; `.githooks/pre-commit`; `scripts/install-hooks.js`; `audit.js`; `nixpacks.toml` (unchanged) | **Done — three layers:** (1) **`prestart: node bundle.js`** — Railway runs `npm start`, so the bundle is regenerated from source **before every serve** → prod drift is structurally impossible (no terser, so `--production` is fine). (2) **`bundle:check` (`node bundle.js --check`)** — rebuilds in memory, EOL-normalized diff, exit 1 on drift / 0 synced, writes nothing — wired into (3) a **committed, self-installing pre-commit hook** (`.githooks/pre-commit` + `postinstall`→`git config core.hooksPath .githooks`, guarded to no-op off-git) that **auto-regenerates + re-stages** the bundle so a commit can never carry a stale bundle, surviving a fresh clone. Deleted the two dead `.min.js`; dropped the unused `terser` devDep; repointed `audit.js` Check 1 to compare **bundle-vs-sources** (real drift). Verified 10/10: hook persists via `core.hooksPath`; synced baseline; `bundle:check` exits 1 on a staled bundle and prestart regenerates it back byte-identical; the hook auto-fixes a forgotten rebuild. **Deferred perf option (Step 6):** serving **minified** bundles is a real ~40% payload saving (`finflow-bundle.js` 313KB→186KB, `app-main.js` 353KB→255KB) — reintroduce terser **then** as an intentional wired build step (`build: node bundle.js && terser …`) + switch index.html to `.min.js`, not as a vestigial artifact. **Refinement (tracked, not fixed):** the pre-commit hook regenerates the bundle from the **working tree**, so it can bake another commit's **uncommitted-but-unstaged** wiring edits into an unrelated commit's bundle — this actually happened (commit `2a70564` baked F31's uncommitted `medium.js` edit into its bundle, creating a transient source-bundle desync in HEAD; healed by the F31 commit). The hook should **warn or refuse** when regenerating with dirty unstaged wiring sources. | both (agent corrected severity) |
| F14 | ✅ **FIXED** (was 🟡 Medium) | `/reports/profit-loss`, `/balance-sheet`, `/cash-flow` ignored entity scope; `cash-flow` also queried **non-existent `receipts`/`payments` tables** (error swallowed → legs always empty) | `server.js:2779,2808,2826,2829` | **Done (Step 3):** all three routes entity-scoped via `req.entityId` + `matchEnt`; totals come from canonical `computeBooks` so the bottom line reconciles with the dashboard/`/books` (P&L now shows COGS + gross + payroll + net). `cash-flow` fixed to real tables `sales_receipts`/`payments_made` (+`payments_received`) and the `.catch(()=>[])` **removed** so a future bad table throws loudly instead of silently emptying legs. Cash-flow stays **cash basis** (no COGS/payroll accrual — decision #2). P&L report modal updated (`app-main.min.js` regen). Live-DB 18/18. | both |
| F15 | ✅ **FIXED** (was 🟡 Medium) | Month-keyed report rows sorted **lexically** (`"Apr '25, Dec '25, Feb '25…"`), mixing order and years | `server.js:2798,2847` | **Done (Step 3):** profit-loss + cash-flow now key months by `YYYY-MM` and sort on that, formatting the `MMM 'YY` label at render. Verified chronological across a year boundary (`Dec '25 → Jan '26 → Feb '26 → Apr '26`). | agent |
| F16 | ✅ **FIXED** (was 🟡 Medium) | Accountant credential "verification" was an always-pass mock (any ≥6-char number); a session was set **before** approval, and login/`requireAccountant` never checked status → unapproved accountants had full portal access | `accountant-routes.js:124,262` | **Done (Step B):** removed the pre-approval session at register (returns `pending-review`); login gates on `status='verified'` (pending/rejected/suspended → 403, no session); `requireAccountant` re-checks status from the DB **every request** (admin suspend revokes immediately); killed the fake auto-verify (server mock, `verify-membership` verdict, and the register page's "Verify →" simulation → honest "manual review, no automatic approval"). Directory already filtered `verified` (not listed until approved). Honest labels: "Verified by FinFlow" vs "Pending review." Verified 19/19 (pg-mem). | both |
| F17 | ✅ **FIXED** (was 🟡 Medium) | Earnings ledger recorded **100%** of a client bill as accountant earnings (ignored Stripe's cut); no commission recorded (`service_commission` never created); tier scale duplicated inline in 4 places | `accountant-routes.js:1434,747`; `admin-routes.js:311` | **Done (Step D):** new `tier-config.js` single source (Bronze→Elite, referral months hard-capped 12, first-3 clients 0% commission); collapsed all 4 inline tier copies; "active client" = consented AND paying; referral months frozen at approval from the ladder; `bill-client` records the full split (`amount_cents`=net, +`billed/commission/stripe_fee_cents`), `application_fee`=tier commission + `on_behalf_of`; `payment_intent.succeeded` reconciles the real Stripe fee. Verified 21/21 (pg-mem). **Stripe-gated live checks in the Railway section.** | both |
| F18 | ✅ **FIXED** (was 🟡 Medium) | **4 of 5 Anthropic call sites were uncapped** — only auto-categorize enforced `ai_usage`. Unprotected: `/api/ai` (chat, escalates to Sonnet), `/api/ai/scan` (**Sonnet vision**, worst cost), `/api/accountants/extract-resume`, `/api/accountants/clients/:id/ai-insights`. Uncapped Anthropic spend the moment the key went live. | `server.js:2613,2775`; `accountant-routes.js:367,1023`; `ai-cap.js` | **Done (F18 step):** new `ai-cap.js` single source — two per-account **monthly budgets**, env-overridable: **shared** (chat + auto-categorize + insights) trial 30 / pro 500 / business 2000 / accountant 300; **scan** (receipt vision + resume parse) trial 3 / pro 15 / business 50 / accountant 10. Every one of the 5 callers now checks the cap **before** the `fetch` (402 `AI_CAP_REACHED` when over) and increments **after** a successful call; cache hits stay free (cap checked only on cache-miss); **fail-closed** (a DB error reading usage → 503, never an Anthropic call); Pro vs Business kept distinct. User budgets in `ai_usage` (`query_count`=shared + new `scan_count`); accountants in new `accountant_ai_usage`. Verified 18/18 (pg-mem): ladder, both budgets independent, over-cap makes **zero** Anthropic calls, pooling, fail-closed. **Live Haiku smoke test is key-gated → Railway section.** *(Also noted: `/api/ai/extract-document` in `LARGE_PAYLOAD_PATHS` is a phantom — no route; harmless dead config.)* | agent |
| **F21** | ✅ **FIXED** (was 🟡 Medium) | **Admin broadcast sent nothing** — counted the audience, logged to `admin_log`, returned `sent: N` as if delivered | `admin-routes.js:517` | **Done (Step E):** the route now selects real recipient **emails** per audience and actually delivers via Resend (chunked, `Promise.allSettled`), returning the TRUE `{sent, failed, total}`. When Resend isn't configured it returns `{sent:0, logged:true}` and the admin toast says "logged only — email not configured" (the draft is kept), never a fake delivery. Soft-deleted users (`data->>'deleted'`) excluded on **every** audience (test caught this — previously only `all_users` excluded them). Subject/message required (400 otherwise). Verified (pg-mem + logic). **RESEND-gated live check in the Railway section.** | **chat add** |
| **F22** | ✅ **FIXED — verified live** (was 🟠 Confirmed-live) | **CSRF-write on form-encoded mutations.** A logged-in victim on an attacker page could have a forged cross-site `<form>` urlencoded POST execute a write: state-changing routes were auth-gated but had **no CSRF token / no Origin check**, `express.urlencoded` was app-wide so the server acted on form bodies, CORS blocks credentialed fetch/JSON but **not** a "simple request" `<form>` POST (no preflight), and the session cookie was `sameSite:'none'` in prod so it rode cross-site posts. | `server.js:291` (cookie), `309-336` (guards) | **Done (F22 step, commit `e293931`):** (1) **`sameSite:'lax'`** — SPA + API are same-origin (one Express app, relative `/api`), so the cookie still rides same-site use and the top-level Stripe-Checkout redirect, but not a cross-site form POST → kills the vector. `secure` unchanged. (2) **Defense-in-depth on `/api` mutations** (before auth; raw Stripe webhook registered earlier, untouched): **content-type gate** (non-JSON, non-empty → 415, closing the simple-request bypass) + **Origin gate** (explicit foreign Origin → 403; missing Origin allowed so webhook/cron/mobile work). Allowlist derived primarily from `req.headers.host`, so it always contains the real serving origin regardless of a stale `APP_URL` (see F29). **Live re-verified on `finflow-production-dab1`:** forged urlencoded → **415**; forged foreign-Origin JSON → **403**; real serving-origin JSON (no cookie) → **401 not 403** (gate allows the live origin — Railway passes the public Host); **positive test — authenticated same-origin write as the Saige owner saved + persisted, `connect.sid` = `SameSite=Lax`**, confirming all three guards are transparent to real users. | **chat add → fixed & verified live** |
| F19 | ✅ **FIXED** (was 🟢 Low) | DB pool `ssl.rejectUnauthorized:false` in prod (MITM risk, bounded on provider private network); fabricated `${fname}.${lname}@company.com` team emails shown as real — plus (post-Step-1) a fabricated RBAC role badge on payroll rows that had no membership/access at all | `database.js:20`; `server.js` `/api/team` | **Done (both halves):** **TLS** — new `dbSsl()` helper: if `DATABASE_CA_CERT` (PEM) is set → `{ ca, rejectUnauthorized: true }` (real verification); otherwise **default is byte-unchanged** (`{rejectUnauthorized:false}` in prod) + a one-time boot warning + a risk-note comment. **Deliberately NOT flipping the default to verify** — doing so without the matching CA makes pg refuse Railway/Supabase's self-signed cert → boot crash (the known prod-crash trap). Additive/non-breaking; the documented path to tighten is to drop the provider CA into `DATABASE_CA_CERT` (no code change, no redeploy roulette). **Roster** — `GET /api/team` no longer injects payroll employees; roster = owner + real invited members (actual `team_members` rows). Kills both the invented email AND the false role badge. Payroll employees live on the Payroll page. Server-only (bundled `renderTeam` just displays server data — no two-engine change). Verified: TLS all 3 branches (dev=false; prod-no-CA unchanged + warning once; prod+CA verifies); roster 8/8 (zero fabricated emails/roles, owner + real members preserved, payroll absent). | both |
| F20 | ✅ **FIXED** (was 🟢 Low) | Dead `db.*` helpers (0 callers) carrying full-table-scan pattern; `42P01→_ensureTable→[]` swallowed typos (hides F14 — a missing/renamed/typo'd table returned silent empty instead of throwing) | `database.js` | **Done:** removed **7** dead helpers (`get/getByUser/all/allByEntity/update/delete/upsert` — audit's "6" missed `allByEntity`). Hardened the swallow (approach B, safe): `_ensureTable` returns a bool + **throws on CREATE failure** + loud `⚠️ WARN` on self-heal; `allByUser`/`insert` **self-heal only ALLOWLISTED tables** (which are all created in `initDB`'s loop, so real tables never crash) and **throw on non-allowlisted/typo `42P01`** — the F14-class silent-empty is gone. `personal_accounts`/`snapshots` (custom-schema, explicit in initDB, not allowlisted) correctly throw on genuine absence. **S3 (referral):** an invalid/unverified `?ref=` code was a silent no-op in the F10/F11 funnel — now logs a distinct warning (dead link surfaced) while signup still proceeds; valid-code path unchanged. Verified 7/7 (mocked pool): self-heal+WARN, typo throws, real data flows, dead helpers gone. **Follow-up:** the 3 reporting-zeros routes (cashflow/reports/tax-filing) still catch→$0 — tracked as F31. | agent |
| **F23** | ✅ **FIXED — live-verified (sweep `e1319ef`, 19 Jul)** — was 🟢 Low | **`banking` rows use `type`/`date`** while the rest of `personal_transactions` uses `tx_type`/`tx_date` — same table, two schemas; date/type filters skip banking rows | `server.js:2623` | Standardize on `tx_date`/`tx_type` | **chat add** |
| **F24** | ✅ **FIXED** (was 🟡 Medium) | **Multi-entity Consolidated P&L was not wired to real data.** The table only populated the *active* entity's figures (others showed $0), and COGS was **hardcoded `cogs:0, grossProfit:totalRev`** so gross profit shown = revenue | `finflow-api-wiring-medium.js:1029-1038` (hardcode at `1033`) | **Done (Step 4):** the `renderEntities` override now fetches **`/api/reports?entity_id=` for EVERY entity** (canonical `computeBooks` — real revenue/COGS/grossProfit/opex/netProfit incl. FIFO COGS + payroll accrual), populates each `entity.data`, then re-renders the consolidated table + KPI cards from `getConsolTotal`. Hardcoded `cogs:0` removed; guarded against fetch loops. Verified in the regenerated bundle. | **agent add (F6)** |
| **F25** | 🟡 Medium | **"Year" period is not a true fiscal year.** The canonical basis (frontend `computeRevenue`/`computeExpenseBreakdown`, and now server `computeBooks`) treats `year` as **all-time revenue/expenses** while payroll is **YTD-accrued** (`monthlyGross × (currentMonth+1)`) — an inconsistent window (all-time vs YTD) mislabeled "year". | `app-main.js:1572,1636`; `server.js computeBooks` | Apply a proper fiscal-year window **consistently across both engines** (revenue, expenses, AND payroll all bounded to the same period). **Deliberately deferred** — mirrored faithfully in `computeBooks` so the report-reconciliation cluster (F7/F9/F14/F24) reconciles first; fixing the window divergently would defeat that. | **agent add (F-cluster Step 1)** |
| **F26** | 🟡 **PARTIAL — inserts FIXED & live-verified (sweep `e1319ef`; new receipt carries `entity_id`, legacy row still `null`); backfill of legacy rows + `computeBooks` entity-scoping still open** | **`sales_receipts` / `payments_received` have no `entity_id`** — they're user-scoped only. So for multi-entity users, user-level receipts/payments-received are attributed to whichever entity is being viewed. Exact for single-entity users (the common case); a real gap for multi-entity. Both engines share this limit (frontend loads them user-wide; `computeBooks` mirrors it). | `server.js:1961,1999` (inserts omit `entity_id`); `computeBooks` | **Data-model migration**: add `entity_id` to `sales_receipts` + `payments_received`, backfill, and scope them like invoices/expenses. Until then, documented as a known user-level limit. | **agent add (F-cluster Step 1)** |

| **F27** | ✅ **FIXED** (was 🟢 Low) | **Dead "Client Books" modal in `accountant-dashboard.html`** — a full duplicate of the client-books view (KPIs, P&L, tax, payroll, etc.) **never opened**: `viewBooks()` redirects to `/accountant-client`, `_booksData` never fetched. Its `renderBooks` still carried the **R11 unpaid-as-income** recompute (inert, but a revival hazard). | `accountant-dashboard.html` | **Done:** removed **616 lines** — the modal markup + all renderers (`renderBooks`/`applyBooksFilter`/`renderEntityTabs`/`switchEntityTab`/`renderBooksPayroll/Balance/Journals/Customers`), journal helpers, `saveBooksNotes`/`flagTransaction`/`exportClientBooks`, and the `_booksData/_booksUserId/_booksClientName` vars. Kept the live `viewBooks` (redirect), the shared `fmt` (also used by the live billing modal), and `logout`/`showPage`/`toast`. Verified: zero orphan references, inline JS parses (`node --check`), the 2 live "View books" buttons still call `viewBooks`. The R11 revival hazard is gone. | **agent add (F-cluster 2b)** |

| **F28** | ✅ **FIXED** (was 🟠 High) | **Marketplace displayed self-declared credentials/memberships beside a "✓ Verified" badge, and `accountants.html` copy claimed automatic/instant verification** — the client directory showed the accountant's own unchecked `credentials`/`memberships` text next to a hardcoded verified badge, and the marketing copy promised automatic/instant verification (contradicted F16's manual admin review). Unchecked claims were presented to clients as if FinFlow confirmed them → false trust signal on the surface clients use to make hiring decisions. | `public/index.html` (directory cards ~6803-6842); `accountant-routes.js:1219`; `public/accountants.html` | **Done (Step G):** new admin-set `accountants.confirmed_credentials` (written at approval from the reviewed Step-F document, `COALESCE`-guarded so reject/suspend never wipe it) — distinct from the self-declared `credentials`/`memberships`/`verification_data`, which are now **admin-only** (dropped from the `/directory` payload). The client card shows ONLY the confirmed line ("✓ … · verified by FinFlow") when set, badge-only otherwise; **🛡 FinFlow Protected** now renders only when `stripe_onboarded` (payments actually run through FinFlow); the bio fallback no longer injects "Verified". Copy fixes align the marketing page with **F16** (removed "automatic/instant registry check" → honest manual review) and **F17** (stale flat "4%" → tiered 10%→2%, 0% on first three clients). Credential document stays client-invisible (two `requireAdmin` readers only). Reviews/ratings confirmed **real, not phantom** (gated to actively-linked clients). Verified 15/15 (pg-mem). | **agent add (Step-F marketplace investigation)** |

| **Step F** | ✅ **DONE** (capability, closes an F16 gap) | Accountant **credential-proof document upload** — before this, F16's "manual review" had no actual document to review (registration collected only self-declared text; a CV picker fed Claude autofill then discarded the file). | `accountant-routes.js` register; `admin-routes.js`; `database.js`; `public/accountant-register.html`; `public/admin.html` | **Done:** new accountant-scoped `accountant_documents` table (base64-in-Postgres, reuses the existing mechanism — no object storage); register validates (PDF/JPEG/PNG/WebP/doc/docx, ≤5 MB) and persists the proof in the **same transaction** as the accountant row (present-but-invalid → 400, nothing created; absent → still registers) + `admin_log` audit; admin review modal fetches a metadata list (file_data stripped) and streams the file **inline** for review, scoped by `accountant_id` (a doc id can't fetch another accountant's file). Registration page label is honest: manually reviewed, not auto-verified, **never shown to clients**. Verified 10/10 (pg-mem: scoped persist, metadata-strip, download + cross-accountant 404, invalid-doc rejects whole registration, no-doc path). | **agent (Step F)** |

| **F29** | ✅ **FIXED** (was 🟠 High) | **Stale `APP_URL` fallbacks send dead links in real emails.** Sites hardcoded `process.env.APP_URL || '<stale domain>'` where the fallback was a dead host — `finflow-production-8e57`/`0817` (Railway edge 404), `app.finflow.io` (no DNS), or `finflow.app`. Inert **only if** `APP_URL` is set on Railway; if unset, accountant application/approval/request-access/login, referral-invite, password-reset, team-accept, and Stripe-connect return/refresh links all pointed at unreachable domains. Confirmed live: the current instance is `finflow-production-dab1` — none of the fallbacks pointed at it. | `accountant-routes.js:300,705,1331,1407,1474`; `admin-routes.js:185`; `server.js:511,2458`; static: `public/landing.html:246`, `public/sitemap.xml`, `.env.example` | **Done:** new `app-url.js` single source — `appUrl()` returns `process.env.APP_URL` (trailing-slash-normalized), falling back to the live `dab1` origin (never a dead domain); `warnIfUnset()` logs a loud one-time boot warning. All **8 runtime link sites** rewired to `${appUrl()}` (the audit's original "five" undercounted — actual set: 5 in accountant-routes incl. the referral-invite `finflow.app` link + Stripe Connect, 1 admin approval, 2 server reset/team-accept). The 3 static files that hardcoded dead hosts also normalized to `dab1` (SEO + mockup + example). Left untouched: `server.js:387` (host-derived, already correct) and `server.js:333` (F22 allowlist, host-derived — confirmed independent of `APP_URL`). No bundle regen (zero literals in bundles). Verified 14/14 (harness): `APP_URL` set → all 8 compose from env incl. trailing-slash normalization; unset → all 8 fall back to `dab1` + warning fires exactly once. Grep confirms **zero** dead app-link literals remain (residual `noreply@finflow.app` is the EMAIL_FROM sender, not an app link; one `server.js:296` comment documents the dead DNS). | **agent add (live re-verification)** |

> **⚠️ F29 custom-domain swap — TWO places, not one.** When the custom domain is purchased, updating the link origin requires **both**: **(1)** the `APP_URL` env var on Railway **+** the `LIVE_FALLBACK` constant in `app-url.js` — this covers all **8 runtime link sites** (emails, Stripe return/refresh, reset, team-accept); and **(2)** the **three static files that now hardcode `dab1`** and do **not** read `APP_URL`, so they will **not** auto-update: `public/sitemap.xml` (SEO canonicals — will otherwise silently keep pointing search engines at the Railway domain after launch), `public/landing.html:246` (browser-chrome mockup text), and `.env.example` (docs). Miss step (2) and the app works but SEO canonicals stay wrong.

| **F30** | 🟢 Low | **Permissions matrix UI is display-only.** The owner-facing role-permissions grid (`public/index.html`) reads/writes `/api/permissions` (persisted to `user_settings`) but **nothing enforces the saved per-account edits** — server-side RBAC (F5 Step 4) enforces a **fixed code matrix** in `rbac.js`. To avoid shipping a cosmetic lie, the grid was relabeled **read-only "role defaults"** (checkboxes disabled, Save removed) as the honest interim. | `public/index.html`; `server.js` `/api/permissions`; `rbac.js` | **Make the UI genuinely functional:** have `requirePerm` merge the account's saved `/api/permissions` overrides **over** the `rbac.js` code defaults (owner always all; unknown/missing → code default; fail-closed on read error), then re-enable editing. Needs the 9 UI rows mapped 1:1 onto the code capabilities so a toggle actually changes enforcement. | **agent add (F5 Step 4 follow-up)** |

| **F32** | 🟢 **REVENUE SIDE FIXED & LIVE-VERIFIED (issue-based accrual, both engines — Stage 1 `b4c904a` + F38 cluster through Step 5, live-verified 20 Jul on dab2); RESIDUAL: `/api/cashflow` reconciliation to the chosen cash meaning + the Store A $1,000 row (blocked on owner ruling)** — was 🔴 CRITICAL | **Revenue recognition is broken two ways at once — the two engines DISAGREE; it is NOT a clean automatic 2×.** The formula `revenue = Σ(paid invoices P) + Σ(sales_receipts R) + Σ(payments_received M)` lives in both `computeBooks` (`server.js:3726-3741`) and `computeRevenue` (`app-main.js:1635-1650`). **Server OVER-counts (P+R+M).** **Client UNDER-counts (P only):** `computeRevenue` **and** the dashboard chart read `window._receipts` / `window._paymentsReceived`, but the loaders assign `window.receipts` / `window.paymentsReceived` (**no underscore**) — the underscore globals are assigned **nowhere** (grep-confirmed across all `.js` + `index.html`), so the R and M legs are permanently `[]` on the client, and every cash sale-receipt / standalone payment shows as **$0** on the dashboard. Double-counting is **conditional** (the *same* sale landing in two legs — e.g. an invoice marked paid **and** given a payments_received row), **NOT** the automatic $1,000→$2,000 the prior row claimed: creating a `payments_received` row does **not** mark the invoice paid (Store A `/api/payments-received` `server.js:2133` never touches invoices; only Store B `/api/invoice-payments` `server.js:3443` → `recalcInvoiceStatus` `3417` flips status, and revenue does **not** read Store B). **F7's dashboard==/api/reports reconciliation held ONLY because the test data had zero sales_receipts and zero payments_received** — add either and the two engines diverge (client stays low, server jumps + double-counts on overlap). A **fourth**, separate revenue definition exists: `/api/cashflow` (`server.js:3099`) uses paid-invoices-only inflow and ignores R and M. **No fifth path:** d-rev is written only by app-main `updateDashboard`=`computeRevenue()` (paid-only, entity-scoped `_realInvoices`); the bundle's `updateKPIs` no longer overwrites it (F7 verified live in `finflow-bundle.js`); invoices default `status='pending'` (`server.js:843`) so unpaid never leak in. (= PRE_LAUNCH #1.) | `server.js:3726-3741`; `public/app-main.js:1628-1653`; dashboard chart `public/finflow-api-wiring-dashboard.js:74-179`; report routes `server.js:3210-3212`,`3259+`,`3099` (+ bundle copies, F13-regenerated) | **DECIDED — ACCRUAL, ISSUE-BASED.** Revenue recognized when an invoice is **issued** (Dr AR / Cr Revenue); `invoice_payments` are **settlement** of AR, never revenue (Dr Cash / Cr AR); `sales_receipts` are cash sales with no invoice (Dr Cash / Cr Revenue) — makes the Outstanding/AR card meaningful. Cash basis becomes a future toggle. Payments route to **Store B** (`invoice_payments`; flips status via `recalcInvoiceStatus`; supports partials). Store A (`payments_received`) → **migrate + deprecate** (see F32 decision note below). Fix the client `_receipts`/`_paymentsReceived` name mismatch and reconcile `/api/cashflow` to the chosen "cash" meaning. **Wiring sources ONLY — never hand-edit `finflow-bundle.js`.** **Test across every period AND both engines:** issue an invoice → revenue +amount **immediately, before payment**; record partial then full payment via Store B → revenue unchanged, AR draws down, status → partial → paid; a sales_receipt → revenue +amount; dashboard == /api/reports == /books at month/quarter/year. | **agent-VERIFIED this session (code-read + writer-chain trace). Prior "$1,000→$2,000 automatic 2×" mechanism CORRECTED → server-over-count vs client-under-count (dead `_`-globals) that disagree. = PRE_LAUNCH #1** |

| **F33** | 🔴 **CRITICAL — OPEN (REPORTED, live-observed; root cause NOT verified)** | **Dashboard KPIs not period-filtered.** Live: Revenue shows **$19.0K identically** in Jun 2026, Jul 2026, Aug 2026, Q4 (Oct–Dec), and Full Year — **all-time in every view**. Expenses ARE period-aware ($8.0K month / $50.0K year), so **revenue and expenses use different period logic** → Net Profit is **meaningless in Month and Quarter**. The revenue **CHART** filters correctly (full bar Jul, empty Jun/Aug) while the **KPI card doesn't** — separate code paths. **Quarter doesn't filter at all.** Code-level, both `computeBooks` (`server.js:3728-3740`) and frontend `computeRevenue` (`app-main.js:1636-1647`) **do** branch by period (year branch has NO date filter → all-time; quarter branch filters on `due_date` only) — so this is **not** a simple missing filter. **Same-screen companions (all REPORTED, unverified):** "vs prior period" deltas wrong (Aug shows ↑700% expenses / ↓39% net with values **identical** to Jul, which showed 0%); expense breakdown doesn't reconcile with the Expenses KPI (~$1.0K breakdown total — Salaries $1.0K, Rent/Software/Marketing $0 — vs $8.0K on the card; Salaries $1.0K when payroll gross is $5K/mo); currency toggle does nothing (**now split out as F34**); Investments shows **$35.1M** on a business with $19K revenue (verify real vs garbage). **F25 ("Year = all-time revenue + YTD payroll") badly understated this.** | dashboard period wiring; delta calc; expense-breakdown vs KPI; investments card; `app-main.js computeRevenue`; `computeBooks` | Repro live with dated test data spanning >1 month; confirm the period toggle re-invokes the revenue **KPI** compute (not just the chart) and that `date`/`due_date` are populated; fix the failing layer. **Do not assume missing-filter — the branches exist in code.** Investigate the companions (deltas, breakdown reconciliation, investments — currency is F34) as part of the same period/wiring pass. | **REPORTED (live), NOT agent-verified — needs investigate→verify pass** |

| **F34** | 🟠 **HIGH — OPEN (REPORTED, not verified)** | **Currency toggle non-functional — mislabelled money.** The USD selector in the dashboard header does nothing; figures **don't convert** when a different currency is selected. Distinct from F33's period-filtering bug — needs its own investigation. Matters more than a dead control: FinFlow already stores **per-row native currency** and normalizes at aggregation via `_safeFX` (the personal-finance currency work), and targets **multi-currency markets** (Caribbean/LatAm/Europe). If the toggle **relabels** figures without converting them, that's **mislabelled money** — same class as F31's fabricated `$0` and F32's double-count. | dashboard header currency selector; `_safeFX`; business-figure aggregation path | Investigate: (a) what the toggle is actually wired to; (b) whether **any** conversion path exists for **business** figures (not just personal); (c) whether the displayed currency **label is accurate** when the underlying number is unconverted. Fix so either the figures convert or the label honestly reflects the un-converted currency — never a relabel-without-convert. | **REPORTED (live), NOT agent-verified — needs investigate→verify pass** |

| **F35** | ✅ **FIXED — LIVE-VERIFIED 20 Jul on dab2 (Step 5 `9937966`)** — was 🔴 HIGH | **"Record Payment" button is broken.** Three colliding `openRecordPaymentModal` definitions: `index.html:4144` (opens the partial-payment modal → `recordPayment()` → **Store B** `/api/invoice-payments`, flips status, uses the invoice id) vs `finflow-api-wiring-final5.js:134` and `finflow-api-wiring-pages.js:256` (open the payments-received modal → **Store A** `/api/payments-received`, no status flip, **ignore all three args**). `finflow-bundle.js` loads `defer` (`index.html:6247`) so it runs **after** the inline Store B def and overwrites it; within the bundle pages.js (`3581`) wins over final5.js (`2956`). **Net: clicking Record Payment on an invoice opens a BLANK payments-received form and, if filled, writes a Store A row that never marks the invoice paid → the invoice stays "outstanding" forever, and the correct Store B / partial-payment path is unreachable from the UI.** | `public/index.html:4144`; `public/finflow-api-wiring-final5.js:134`; `public/finflow-api-wiring-pages.js:256`; button `public/app-main.js:1981` | Collapse to **ONE** `openRecordPaymentModal` that passes invoice id/client/amount through and routes to **Store B** per the F32 decision; delete the Store A openers. **Confirm first with a 30-sec live-browser check of which modal actually opens** (load-order reasoning). Ships together with F32. | **agent add (F32 investigation)** |

| **F37** | ✅ **FIXED — live-verified (sweep `e1319ef`; `todayLocal()` returned 07-19 vs UTC 07-20, actively correcting a live off-by-one)** — was 🟡 Medium | **Date generation is timezone-inconsistent — some paths stamp UTC, others local.** Default record dates are set with `new Date().toISOString().slice(0,10)` (**UTC** calendar date) in many places (`app-main.js:2998,3031,3065,3123,3809,4577`, journal `965`, audit filename `839`), while display/stamps use `new Date().toLocaleDateString(...)` (**local**). For any user whose local date differs from UTC at the moment of action, the two disagree → the QA report's "P&L modal **Generated 7/18/2026**" while a same-session receipt **saved as 2026-07-19**. Not fixed by the F33 period-window unit (that's period *filtering*; this is date *generation*). | `public/app-main.js` (the `toISOString().slice(0,10)` default-date sites vs the `toLocaleDateString` stamps) | Standardize default record dates + stamps on ONE convention — local calendar date (`YYYY-MM-DD` from local Y/M/D), since that's what the user sees and enters. A shared `todayLocal()` helper, applied to every default-date site. | **agent add (F33 build / QA)** |

| **F33 / F25** | ✅ **FIXED — VERIFIED LIVE (19 Jul 2026)** | **Period-window unit (F33 + F25) built this session.** One canonical `_periodWindow(period, monthIdx)` (fiscal-year indexed, honors the stepper) feeds BOTH client KPI engines (`computeRevenue`/`computeExpenseBreakdown`) and the server (`computeBooks` gains an explicit `{start,end,elapsedMonths}` window mode; `/api/reports` accepts `?start&end&elapsedMonths`, validated, backward-compatible). Deltas now come from the same windowed compute (kills the ↑700%/↓39%). Quarter = the actual fiscal quarter (not hardcoded Q4). **Year = fiscal year, all legs incl. payroll × elapsed fiscal months (F25)** — no longer all-time revenue vs calendar-YTD payroll. **Stage-1 gap logged honestly:** F32 Stage 1 missed a SECOND monthly-array builder in `app-main.js` (paid-only / `due_date` basis, fiscal-indexed) that competed with the dashboard's — it's now **deleted** and unified to one fiscal, issue-based builder. Also fixed the `currentMonthIdx` default (was fixed `11` = Dec under a Jan fiscal year → the QA "selected period showed Dec 2026" bug). Verified: jsdom stepping (Jun/Jul/Aug distinct, empty month = $0, real delta baseline) + pg-mem `computeBooks` window mode + **dashboard == /api/reports at month/quarter/year** + backward-compat string 'year' = all-time + F25 Year before/after (all-time 11,200 → fiscal 6,200 on the test set) — 18/18. | `public/app-main.js`; `public/finflow-api-wiring-dashboard.js`; `server.js` `computeBooks` + `/api/reports` | **✅ LIVE-VERIFIED** on `finflow-production-dab1` @ `146019c` (19 Jul 2026, authenticated browser step-through, read-only — no writes). **Clean data confirmed first:** the QA row (SR-0761, $150) was **already absent** — `sales_receipts` held a single $40 row, so Year read exactly $19,040 with no deletion needed. **All on-load tells pass:** Month view opens on **Jul 2026** (not Dec — the `currentMonthIdx` default fix is live); Quarter label = **"Q3 · Jul 2026 – Sep 2026"** (not the old hardcoded Q4); Year = "Full Year · Jan 2026 – Dec 2026". **Revenue steps correctly across the whole surface:** Jun **$0** / Jul **$19.0K** / Aug **$0** / Q3 **$19.0K** / Year **$19.0K**, with the KPI card, the `overviewChart` revenue series (`[…, Jul 19040, …]`, Jun/Aug empty) and the header label **all moving together**. **dashboard == `/api/reports` at EVERY period for revenue, expenses AND net** — Jun `0/7000/−7000`, Jul `19040/8000/11040`, Aug `0/0/0`, Q3 `19040/8000/11040`, Year `19040/50000/−30960`. ⚠️ **Verification gotcha (cost one false alarm):** `/api/reports` must be called with the **per-window** `elapsedMonths` (1/1/0/1/7 for Jun/Jul/Aug/Q3/Year), **not** the fiscal month index — passing the index scales the payroll accrual and produces a bogus expense mismatch (June "$42K"). The app was right; the param was wrong. **Formula sanity-checked, not merely reconciled (methodology rule):** $19,040 = invoices $10,000 + $5,000 + $4,000 (all `paid`, all issued July 2026) **+** the $40 sales receipt — i.e. issue-based accrual with `payments_received` correctly absent (F32 Stage 1 holding). **F25 confirmed:** Year expenses $50K = payroll × **7 elapsed fiscal months** + $1K opex — a true fiscal window, no longer all-time-revenue vs calendar-YTD-payroll. **Not covered this pass:** the accountant `/books` leg (only dashboard ↔ `/api/reports` was exercised). | **agent (F33/F25 build; live-verified in browser 19 Jul)** |

> **F33 companions — live-observed 19 Jul during the F33/F25 verification (each still OPEN, needs its own investigate→verify pass; NOT regressions from the period-window unit, whose scope was the revenue window).** (1) **Overview-chart expense series disagrees with the Expenses KPI** — `overviewChart` expenses read `1000` for July while the KPI and `/api/reports` both read `8000`; the ~$7K payroll accrual is **absent from the chart series** but present in the KPI. This pins the previously-vague "expense breakdown doesn't reconcile with the Expenses KPI" companion to a specific surface. Revenue is unaffected (chart and KPI agree exactly). (2) **Expense-breakdown panel renders `$35,140.0K` (~$35.1M)** on a business with $19K revenue — the "Investments $35.1M" companion is **real, not a misread**, and is a live wrong-money-on-screen surface. (3) **Deltas now vary per period** (the frozen ↑700%/↓39% is gone — the windowed-compute fix worked), **but** July shows "↑ 0% vs prior period" against a $0 June, which cannot be right; delta correctness is **not** confirmed. Also seen nearby: "$-5.0M · -12%" and "-163% margin" cards.

| **F36** | ✅ **FIXED — LIVE-VERIFIED 20 Jul on dab2 (Step 2 of F38 cluster, `85a9d2f`; backdated invoice `issue_date 2026-06-15` / `created_at 07-20Z` recognized in June, not July)** — was 🟡 Medium OPEN | **Invoices have no explicit issue date — revenue recognition keys on `created_at`, a row-insert timestamp.** **FIX (Step 2):** added a user-editable `issue_date` to invoices AND bills (default today/local per F37, backdatable; `#inv-issue`/`#bill-issue` inputs wired through both save paths); all five revenue-recognition sites (`computeBooks`, profit-loss, `computeRevenue`, `buildMonthlyArrays`, `updateKPIs`) now key on `issue_date || created_at`. The `created_at` fallback is documented in-code as a **time-boxed transition** (created_at is UTC; at this account's negative offset a first-of-month row can misassign — see F47). No-op on current data (no live row carries issue_date). Harness-verified (no-op equivalence + backdated-precedence + UTC-boundary caveat); **live gate pending** (dashboard==reports==books, backdated-invoice scratch test). F32 Stage 1 (issue-based accrual) recognizes each invoice in the period of its **issue date**, using `created_at` as the proxy. Correct **only** while invoices are entered as they are issued — which holds today (no seed/import/backdate path exists; `seedUserData` was removed). But `created_at` is a data-entry timestamp: the first time a user **backdates** an invoice or a bulk import lands, `created_at` ≠ the business issue date, so revenue falls in the wrong period (and would bake a bug into the F33 period fix). | invoice POST `server.js:843` (no issue-date field); invoice modal `public/index.html:3337` (only a *due* date); recognition keys in `computeBooks` / `computeRevenue` / profit-loss | Add a **user-editable issue date** to the invoice form (default = today, backdatable), persist it, and **key recognition on it** (fallback to `created_at` for legacy rows). Until then `created_at` is the documented, correct-for-real-time-entry proxy. | **agent add (F32 Stage 1)** |

| **F31** | ✅ **FIXED** (was 🟠 High) | **Reporting routes return fabricated `$0` on a query failure — "wrong money on screen" (F3/F7/F14 class).** `/api/cashflow`, `/api/reports`, `/api/tax-filing` each `catch → res.json({...:0})`, so a thrown query error (post-F20, the DB layer throws on a bad table) was re-masked as legitimate zeros on the three most important financial reports. A failure-induced `$0` is indistinguishable from a real `$0`. **Also relocated to the client:** the F24 consolidated P&L left `e.data` unset on `!r.ok` → the table rendered `$0` for a failed entity. | `server.js` (3 catch blocks); `public/finflow-api-wiring-medium.js` (F24 fetch); `public/index.html` (`getConsolTotal`/`fmtTotal`/`renderConsolPL`/`renderEntities`) | **Done (server + client):** the 3 routes now `res.status(500)` instead of fabricated `$0` — the separation is structural (a genuinely-empty period returns real `$0` from the `try`; only a thrown error reaches the `catch`). Client F24: `!r.ok`/failed fetch → `e.data={unavailable:true}` → the entity renders **"—"** + "Failed to load" (F3's honest-missing language, never `$0`); `getConsolTotal`→`null` if any entity is unavailable → consolidated total + KPI cards render **"—"** (a silent partial sum is the same lie); a genuinely-empty entity still shows honest `$0`. Verified: server 3/3 (empty→200+zeros, throw→500, no fabricated revenue), client 9/9 (empty→$0, failed→"—", siblings unaffected, incomplete total→"—"). Bundle regenerated + in sync. | **agent add (F20 follow-up)** |

| **F38** | 🟠 **HIGH — IN PROGRESS (Steps 1–4 committed + live-verified; Step 5 pending) — cluster with F35/F36/Store A** | **Expense/AP recognition is ASYMMETRIC with F32's revenue accrual — unpaid bills overstate net profit (the mirror of F32).** F32 Stage 1 made revenue **accrual** (an *issued* invoice = revenue). But the expense side did NOT migrate: `computeBooks` recognizes bills as an expense **only when paid**, via `payments_made` — an issued-but-unpaid **bill is never a P&L expense**. Meanwhile the balance sheet **does** count unpaid bills as AP (`server.js:3264`). Net: a business holding a $10K unpaid bill shows a $10K liability but $0 expense, while an unpaid $10K invoice **is** counted as revenue → **net profit is systematically overstated by outstanding AP**. This is the exact "two bases in one P&L" class F7/F32 fought, re-introduced on the expense leg. `computeBooks` fetches invoices/expenses/payments_made/payroll/sales_receipts — **no `bills`**; `computeExpenseBreakdown` mirrors it (`_realExpenses` + `_paymentsMade` + payroll, no bills). | `server.js` `computeBooks` (`3739-3797`, expense legs; balance-sheet AP at `3264`); `public/app-main.js` `computeExpenseBreakdown` (`1562-1600`) | Under the chosen accrual basis: recognize an **issued bill** as expense when issued (Dr Expense / Cr AP), and make `payments_made` **settle AP** (Dr AP / Cr Cash), NOT a fresh expense — mirroring the F32 invoice/AR treatment so revenue and expense share one basis. Ships with the F32 completion (Store A / F35 cluster); test net profit == revenue − COGS − (accrued expense incl. issued bills) at every period, both engines. **── CLUSTER PROGRESS (2026-07-19) ──** **Step 1 ✅ committed `2c1d36f`** — Store A read-only inventory tool (`scripts/inventory-store-a.js`); live state confirmed = 1 row, $1,000, empty `invoice_ref` → NEEDS DECISION (blocked, never auto-routed to a cash sale). **Step 2 ✅ committed `85a9d2f`** — F36 issue_date (above). **Step 3 ✅ committed `8ecdbd4`** — `recalcBillStatus` + bill `amount_paid` + `payments_made.bill_id` + `RECOGNIZED_BILL` allowlist + balance-sheet AP = Σ(amount − amount_paid) over recognized non-paid bills; orphan rule (bill_id null → direct expense) in code, inert; pg-mem-verified (issue→partial→paid drawdown, unlinked-payment isolation, user-scoped sum). Steps 1–3 are all **no-op on current data** (0 bills, 0 payments_made) and are being pushed unverified-live by design — the combined live gate runs after Step 4. **Step 4 ✅ committed `63a047d`, LIVE-VERIFIED 20 Jul 2026** — added the issued-bill expense leg at all FIVE accrual sites (computeBooks, profit-loss, computeExpenseBreakdown, buildMonthlyArrays, updateKPIs), both engines, keyed on issue_date, RECOGNIZED_BILL, full amount; reclassify LINKED payments_made out of expense (orphans stay). **AP AMENDMENT to fold into Step 4:** change the balance-sheet AP filter (currently `server.js:3305`, `RECOGNIZED_BILL.has(s) && s !== 'paid'`) → `RECOGNIZED_BILL.has(s)` with reducer `Σ max(0, amount − amount_paid)`, so AP is **arithmetic-driven not status-driven** (a wrongly-set 'paid' can't hide a real liability; an overpayment contributes 0, never negative). Harness cases: bill $500 / amount_paid 200 / status forced 'paid' → AP $300; amount_paid 600 on $500 → contributes 0. **Combined live gate (Steps 2–4):** ✅ PASSED live 20 Jul 2026 on `finflow-production-dab1` (authenticated id 1, read + scratch-create/delete, baseline restored). dashboard == /api/reports at ALL five periods (Jun/Jul/Aug/Q3/Year rev·exp = 0·7000 / 19040·8000 / 0·0 / 19040·8000 / 19040·50000); AP $0; scratch $500 issued bill → expense +$500 immediately BEFORE any payment (server Jul 8500 / Year 50500, client Jul 8500, AP 500), then $200 LINKED payment → expense unchanged 8500 + recalcBillStatus wrote amount_paid 200/partial, AP → 300; both scratch rows deleted → exact baseline restored. Orphan-payment leg (+$75) proven in pg-mem/jsdom harness (live orphan test blocked by write classifier). /books leg reconciles by construction (identical computeBooks), not exercised as owner. **Dead-global fix** `57371f3` (F32-class): client expense engines now read `window.paymentsMade` (the loader-set global) not the never-assigned `window._paymentsMade` — no-op on current data (0 payments), makes the orphan leg live-correct so dashboard == /api/reports holds once a real orphan payments_made exists. Original gate spec: Revenue exactly $19,040; Jun/Jul/Aug/Q3/Year = $0/$19.0K/$0/$19.0K/$19.0K; dashboard == /api/reports == /books; backdated-invoice scratch (issue_date June → lands June → delete → baseline restored); plus an issued-bill scratch (expense +amount before payment, AP reconciles). **Step 5 ⏳ NOT STARTED** — F35 UI collapse (delete Store A openers, invoice Record Payment → Store B `invoice_payments`) + bill "record payment"/"mark paid" must create a **real linked `payments_made` (bill_id set)**, NOT merely set amount_paid (a bare status flip leaves the cash outflow invisible to the cash-basis cash-flow route). Own deploy, own verification. **BLOCKED:** the $1,000 Store A row awaits the owner's ruling on what it represents before the Payments-Received page is frozen read-only. | **agent add (re-audit — new; cluster in progress)** |

| **F39** | 🟢 **Low — OPEN (new; re-audit 2026-07-19)** | **Recurring invoices can never be end-dated — they generate forever.** The recurring-invoice branch of `runRecurringScheduler` (`server.js:2961-2968`) has **no `end_date` guard** (the recurring-BILL branch at `2978` does), and `POST /api/recurring-invoices` (`server.js:2062`) doesn't even accept an `end_date` field — so an active recurring invoice profile keeps materialising `invoices` rows indefinitely with no stop condition. | `server.js:2062` (POST — no `end_date`); `server.js:2961-2968` (scheduler loop — no end-date/stop check) | Accept `end_date` on the recurring-invoice POST/PUT (mirroring recurring_bills) and add the same `if (r.end_date && r.next_run > r.end_date) → status:'completed'; continue;` guard to the scheduler's invoice loop. | **agent add (re-audit — new)** |

| **F40** | 🟢 **Low — OPEN (new; re-audit 2026-07-19; within the F32/cashflow cluster)** | **`/api/cashflow` keys paid-invoice inflow on `due_date`, not the payment date.** `GET /api/cashflow` buckets a paid invoice's inflow by `(x.due_date||'').slice(0,7)` (`server.js:3110`), so cash-basis inflow lands in the month the invoice was **due**, not the month cash was **received**. Root cause: invoices carry no payment-date; settlements live in Store B (`invoice_payments`), which the cash-flow route doesn't read. Approximate today (few partials), wrong once payment timing ≠ due timing. (`POST /api/reports/cash-flow` at `3292` has the same shape, keying on `created_at||due_date`.) | `server.js:3110` (`/api/cashflow` month key); `server.js:3292` (`/api/reports/cash-flow` inflow key) | When F35/Store B lands, key paid-invoice cash inflow on the **`invoice_payments` payment date**, not the invoice due/created date. | **agent add (re-audit — new)** |

---

## Re-audit 2026-07-19 (status corrections + verification)

A full re-audit was run against the live code on 19 Jul 2026 (code-read verification of every open finding). No files were changed by the re-audit except this log. Results:

**Status corrections (the rows above are now stale where they say "pending commit" / "OPEN"):**
- **F32 Stage 1, F33, F25 are COMMITTED and live in code** (commits `b4c904a`, `d39aed4`, `146019c`) — not "pending commit." F32 Stage 1 (issue-based accrual) is genuinely implemented on **both** engines (`computeRevenue` `app-main.js:1661`; `computeBooks` `server.js:3718`), `payments_received` is removed as a revenue leg, and the F32 `_receipts` name mismatch is fixed (`computeRevenue` reads `window.receipts`, which the loaders set — `final5.js:28`, `pages.js:141/190`). The canonical `_periodWindow` (`app-main.js:1636`) feeds both KPI engines + the server (F33/F25). **F32 as a whole remains PARTIAL** — F35, the Store A migration, and cashflow reconciliation are still open, and F38 (above) is a newly-found gap in the same accrual migration.
- **PL#10 "recurring invoices/bills inert" is STALE — the scheduling half is FIXED.** `runRecurringScheduler` (`server.js:2951`) runs at boot + hourly (`server.js:4114-4115`) and materialises `recurring_invoices`→`invoices`, `recurring_bills`→`bills`, and `recurring_personal_transactions`→`personal_transactions`. (The credit-note / vendor-credit **contra** half of PL#10 was not confirmed fixed — treat that half as still open; F39 above is a residual gap in the invoice scheduler.)

**⚠️ THIS BLOCK WAS REFRESHED 20 Jul 2026 — the pre-20-Jul version listed F23/F26/F35/F36/F37/PL#6 as "still open"; they have since SHIPPED. Current truth below.**

**SHIPPED & LIVE-VERIFIED since that re-audit (20 Jul, dab2):** F23 (banking `tx_type`/`tx_date`, sweep `e1319ef`), F26 (entity_id on inserts — sweep; legacy backfill still open), F35 (Record Payment → Store B, Step 5 `9937966`), F36 (issue_date, `85a9d2f`), F37 (`todayLocal()`, sweep), F38 Steps 1–5 (accrual expense leg + AP arithmetic), F48 (Store B ownership/validation/delete-revert, `98ec1a6`), **F48 follow-up (AR now arithmetic — `markInvoicePaid` settles a real `invoice_payment`, boot-backfill of status-only-'paid' rows, AR flipped to `Σ max(0, amount − amount_paid)`; harness-verified, pending live)**. PL#6 (FX settle modal) is structurally fixed but **still unexercised end-to-end** (see its row).

**GENUINELY STILL OPEN (confirmed in current code):** **F34** (`updateCurrency` `app-main.js:4247` swaps symbol only, no conversion), **PL#11** (`calcAndRenderTax` `app-main.js:5907` fabricates `ytdPaid = liability × 0.75` + flat 25%), **PL#4** (`/api/audit-trail` page reads `audit_trail` — 2 writers — while real history goes to `audit_log` via `logAudit()` at 12 sites; audit page effectively empty), **PL#3** (`POST /api/entities` `server.js:801` has RBAC but no plan-limit paywall — confirm server-side 402/403), **PL#5** (no quote→invoice conversion path exists). **F33 companions** still open: overview-chart expense series omits payroll ($1K vs $8K KPI); Investments $35.1M (stale manual prices — live-pricing feed designed, Part 2, awaiting API keys); July delta "↑0%" vs $0 June. **From the 20 Jul health pass:** **F49** (`reload.js` dev script live in prod — failed WS every load), **F50** (= PL#2 boot race — `loadEntityData` double-fires, first-load `$0` flash), **F51** (placeholder pages shown as features — Banking/Find-Advisor/Client-Portal/Templates/API-Connections; tax-filing + team stray "Coming Soon" fragments), **F52** (a11y — 3 fields no id/name, 7 no label). **Corrected:** the QA "AI key leak" is a **misleading 502 error**, not a secret leak (no `sk-ant` value exposed) — downgraded to 🟡. **Health-pass verdict: structurally healthy (47/47 pages render, 27/29 endpoints 200); remaining work is boot-race + dev-script cleanup + honesty pass, not core-money defects.**

**Checked, NOT a defect:** the F33-flagged "Investments $35.1M" is data-driven — `personalPortfolioValue()` = `holdings.reduce((price×shares))` (`app-main.js:3582`); no fabrication in the code path, the figure just reflects entered holdings.

---

## F35 / payments migration — enumeration (2026-07-19, code-read; agent-verified)

Migration-prep detail for the F32/F35 cluster (payments live in two tables — Store A `payments_received` JSONB, Store B `invoice_payments` typed). App code untouched — enumeration + read-only verification only. Runtime-critical claims independently re-checked (two-script load list, bundle source order, orphan route) and confirmed.

**F35 — `openRecordPaymentModal` collision, resolved to the runtime winner.** 3 authored defs, **5 physical** (2 verbatim copies inside `finflow-bundle.js` — grep the tree and you get 5 hits, not 3):
1. `index.html:4144` — `(invoiceId, client, amount)`, opens `record-payment-modal` → `recordPayment()` (`index.html:4171`) → `POST /api/invoice-payments` = **Store B**.
2. `final5.js:134` — zero-arg, opens `modal-payment-received` → `savePaymentReceived` → `POST /api/payments-received` = **Store A**. (bundle copy `2956`)
3. `pages.js:256` — zero-arg `window.…=` assignment, same Store A path. (bundle copy `3581`)

**Runtime winner = bundle:3581 (source `pages.js:256`) — the blank Store A form.** Proof, not intent: only `app-main.js` + `finflow-bundle.js defer` load (`index.html:3481`,`6247`) — the wiring files never execute directly; `defer` runs the bundle after the inline `#1` sets the global; inside the bundle `bundle.js:18-19` fixes `final5` (idx 5) **before** `pages` (idx 6), so the hoisted `#2` decl is overwritten by `#3`'s `window.…=` assignment. **Call site `app-main.js:2019` passes 3 args that are silently discarded** (both button paths — `index.html:2778` and the `6273` shim — hit the zero-arg Store A winner). Collapsed signature must therefore be **`(invoiceId, client, amount)` all-optional** (def #1's shape) to satisfy the call site without breaking zero-arg calls. **Decision still required:** which table survives — keeping def #1 (Store B, real `invoice_id` FK) is the typed target; the current winner writes Store A.

**`payments_received` — the one live money-read that survived F32.** Exactly one functional data path remains: the `POST /api/reports/cash-flow` **inflow** leg (`server.js:3285→3294`), deliberately kept as cash-basis inflow. Everything else is display (`GET` `2131`; client page KPI `pages.js:248`) or comments. **Two stale comments to not be misled by:** the `computeBooks` docstring (`server.js:3707`) still lists "+ payments received" in the revenue formula, but the code below (`3739-3782`) does **not** read the table; the `/api/reports/profit-loss` note (`3218`) correctly records it was dropped. Client `computeRevenue` (`app-main.js:1661-1676`) reads only `_realInvoices` + `receipts` — nothing reads `window.paymentsReceived` for money (display-only). Client too has **two** full Store A CRUD blocks (`final5.js:108-182`, `pages.js:215-294`) — pages.js wins by the same overwrite mechanism.

**Store A → Store B field collision (the migration hazard).** Clean: `amount`→`amount`, `date`→`payment_date`, `method`→`method`. **Broken:** `invoice_ref` (free-text ≤50) → `invoice_id INTEGER` (needs a resolve step; unresolvable refs have no target row) — the core collision; `customer` (required in A) has **no column** in B (B derives client via `JOIN invoices`); `notes` is sent by the client but **dropped server-side** at the A POST (`2134`); A's `entity_id` is always null (F26) so migrated rows land entity-null. **No shared key exists to detect true duplicates** (A has no `invoice_id`, B has no `customer`/`invoice_ref`) — same-payment rows can only be matched heuristically (amount + date + resolved invoice). **Row counts are UNKNOWN — not inferable statically** (live Postgres, no repo fixtures; `seedUserData` removed). A read-only `SELECT count(*)` on both tables, grouped by `invoice_ref` resolvability, is a prerequisite before any migration is drafted. Store B is UI-unreachable today (F35), so real B rows are likely near-zero — unconfirmed.

**Cash-flow routes — reconciliation is per-route, not free.** `GET /api/cashflow` (`server.js:3091`) is **orphan dead code** (verified: no `fetch` caller anywhere outside audit docs) — never read `payments_received`, unaffected by the migration, but carries its own F40 `due_date` basis. `POST /api/reports/cash-flow` (`server.js:3276`, caller `app-main.js:4910`) is the one that **must be reworked**: it currently sums paid invoices + `sales_receipts` + `payments_received` and does **not** read `invoice_payments`; after migration it would **double-count** unless its inflow legs are revised — (a) drop the `payments_received` leg, (b) decide paid-invoice basis (`created_at||due_date` today vs actual `invoice_payments` payment dates = F40's fix), (c) fold migrated cash sales via `sales_receipts` without also counting them through the invoice leg. (The `showPage('cashflow')` dashboard page reads neither route — local accrual arrays — and is clean.)

**Blast radius / edit discipline.** `public/finflow-bundle.js` is a **generated artifact** (`node bundle.js` concatenates the 10 wiring sources; a pre-commit hook auto-regenerates + drift-checks it) — **never hand-edit it.** Edit the sources then regenerate: `final5.js` (Store A CRUD + def #2), `pages.js` (Store A CRUD + def #3 the winner + `savePaymentReceived`), `postgres.js` (payments-received/cashflow refresh routing). Directly-loaded, safe to edit in place: `app-main.js` (call site `2019`, `computeRevenue`, Route-2 caller `4910`), `index.html` (def #1 `4144` = the Store B opener to keep, `recordPayment` `4160`, call sites `2778`/`6273`, the script list), `server.js`, `database.js`.

## F32 decision note (recognition basis — DECIDED 18 Jul 2026)

**Basis: ACCRUAL, ISSUE-BASED.** Revenue is recognized when an invoice is **issued** (Dr AR / Cr Revenue), not when paid. Settlements (`invoice_payments`, and legacy `payments_received`) draw down AR and are **never** revenue (Dr Cash / Cr AR). `sales_receipts` are cash sales with no invoice (Dr Cash / Cr Revenue). This makes the Outstanding/AR card meaningful. **Cash basis** (receipts + collections only) becomes a **future toggle**, not the default. Note: this is a deliberate shift from the app's current *paid-invoice* recognition — under issue-based accrual, an unpaid issued invoice **is** revenue **and** AR.

**Payments route to STORE B** (`invoice_payments`, keyed to a real `invoice_id`, flips status via `recalcInvoiceStatus`, supports partials). The three `openRecordPaymentModal` definitions collapse to one that passes the invoice id/client/amount through (see **F35**).

**Store A (`payments_received`) disposition — options (recommend #1):**
1. **Deprecate + migrate (recommended).** One-time migration of existing rows: if a row's `invoice_ref` matches an invoice → recreate it as an `invoice_payments` (Store B) settlement against that invoice (then `recalcInvoiceStatus`); if it matches nothing → treat as a cash sale → recreate as a `sales_receipts` row. Then stop writing Store A, remove it from the revenue formula, and make the Payments-Received page a read-only "legacy" view (or fold it into invoice payments). *Cleanest under the chosen model; each dollar ends up in exactly one recognized place.*
2. **Keep, reclassified as settlement-only.** Retain the table but remove it from revenue and never let it be a recognition leg — it becomes a manual AR-settlement log parallel to Store B. Weaker: duplicates Store B's job without flipping status, so AR/Outstanding still won't move.
3. **Minimal: exclude from revenue, leave as-is.** Just drop the `payments_received` leg from the formula and leave the page. Smallest change, but leaves a confusing dead-end store that doesn't affect any figure. *Not recommended.*

Migration must be reversible/dry-runnable and gated on my approval — it rewrites historical money rows.

---

## Pre-launch backlog — `PRE_LAUNCH_FIX_PLAN.md` (separate accounting-grade audit, 15 issues)

A separate debugging-session audit (`PRE_LAUNCH_FIX_PLAN.md`, now tracked in-repo) catalogues 15 issues + foundations. **It was NOT investigated during the F1–F31 work** — reconciled here so AUDIT_MASTER stays the source of truth. Its root theme (Foundation #1): KPIs are summed ad-hoc in three files instead of derived from a double-entry ledger — the same drift F7 fought, un-rooted.

> **⚠️ VERIFIED vs REPORTED — read before acting.** Of the OPEN money bugs, **only F32 is agent-verified** (code-read this session). **F33 and every PL item below are REPORTED, NOT VERIFIED** — they come from a live-observation / prior-audit pass, not from a confirmed root-cause investigation. Each needs its **own investigate→verify pass** before it is treated as confirmed or marked fixed. Do not assume the reported root cause is the real one (F33 explicitly warns: the period branches DO exist in code). `PRE_LAUNCH_FIX_PLAN.md` also **predates the reconciliation cluster**, so its narrative sections are stale where they describe KPI math.

| PRE_LAUNCH issue | Sev | Status | Maps to |
|---|---|---|---|
| #1 Revenue double-count (+ under-count from unset `_receipts` globals) | P0 | 🔴 **OPEN** | **F32** (verified) / **F33** (period symptom) |
| #11 Tax Filing shows fabricated figures — `calcAndRenderTax()` ignores `/api/tax-filing`, computes locally with a flat 25% rate + hard-coded `ytdPaid = liability × 0.75` (an invented "amount due") | P0 (🟠 HIGH) | 🔴 **OPEN** | frontend `calcAndRenderTax` — **distinct from F8** (payroll tax engine, removed); F31 fixed only the server `/api/tax-filing` catch, NOT this fabrication |
| #2 Boot race — data only appears after manual refresh (4 uncoordinated boot paths) | P0 | 🔴 **OPEN** | overlaps Step 6 (loaders firing 6–8×) |
| #4 Audit Trail reads wrong/near-empty table (`audit_trail`, 2 of 132 endpoints) vs real `audit_log` | P1 | 🟠 **OPEN** | related to F5 (RBAC `audit:read` gated, but coverage/immutability unaddressed) |
| #3 Multi-entity paywall bypassable / unenforced server-side | P1 | 🟡 **PARTIAL** | `2a70564` "close entity-gate bypass on Add Business" (confirm server `POST /api/entities` 402/403 still needed) |
| #7 Team members fabricated from payroll (fake emails + auto-roles) | P1 | ✅ **FIXED** | **F19** (dropped payroll from roster) |
| #9 Payroll gross column renders black/invisible (`app-main.js:2230`) | P2 | 🟡 **LIKELY FIXED — VERIFY** | `2a70564` "legible payroll gross color" (external commit; not agent-verified) |
| #5 Quotes don't convert to invoices; 3 conflicting quote wirings | P1 | 🟠 **OPEN** | — |
| #6 FX "Settle" button dead (`settleFXTransaction` returns early) | P1 | 🟠 **OPEN** | backend route works; UI unwired |
| #10 Credit/vendor credits not applied as contra; recurring items inert | P2 | 🟠 **OPEN** | — |
| #8 Items/Inventory two parallel systems; reorder point absent | P2 | 🟠 **OPEN** | — |
| #12 Banking "Coming Soon" placeholder | P2 | 🟠 **OPEN** | overlaps Step 7 (honesty pass) |
| #13 Templates static mockup (no persistence) | P2 | 🟠 **OPEN** | overlaps Step 7 |
| #14 Client Portal vaporware (no backend, fake shareable links) | P1 | 🟠 **OPEN** | overlaps Step 7 |
| #15 API Connections static catalog ("Connect" doesn't connect) | P2 | 🟠 **OPEN** | overlaps Step 7 |

**Non-negotiable-before-"accounting product" (per that plan):** revenue recognized once (#1 → F32), no fabricated tax figures (#11), complete immutable audit trail (#4).

### Detailed PL findings — file:line specifics (all REPORTED, NOT verified)

- **PL#11 — Tax Filing fabricated figures (🟠 HIGH).** `calcAndRenderTax()` ignores `/api/tax-filing`, computes locally with a flat **25%** rate and hard-coded `ytdPaid = liability × 0.75` — an invented "amount due." Distinct from F8 (payroll tax engine, removed); F31 fixed only the server catch, not this fabrication.
- **PL#2 — Boot race (🟠 HIGH).** Data only appears after a manual refresh; **4 uncoordinated boot paths**. Overlaps Step 6.
- **PL#4 — Audit trail reads the wrong table (🟠 HIGH).** The page reads `/api/audit-trail` → `audit_trail` (only **2 of 132** endpoints write it, no field-level values); the real history goes to `audit_log` via `logAudit()` (**12 sites**). The page is effectively empty. Needs one append-only, field-level, immutable log covering every financial mutation.
- **PL#3 — Multi-entity paywall (🟠 HIGH, 🟡 partial).** `2a70564` closed the Add-Business bypass. **Still confirm** server-side enforcement on `POST /api/entities` (`server.js:785`) returns 402/403.
- **PL#14 — Client Portal is vaporware (🟡 MEDIUM).** `PORTALS = []` never fetched, no `/api/portal` route exists, `addPortal()` pushes in-memory objects lost on refresh, links point to the non-existent `portal.finflow.io`, "Send email" sends nothing (`index.html:5982-6016`).
- **PL#5 — Quotes never convert to invoices (🟡 MEDIUM).** Three conflicting wirings (`pages.js`/`stubs.js`/`bundle.js`) compute the value card differently. Broken Quote→Invoice→Payment lifecycle.
- **PL#6 — FX Settle button dead (🟡 MEDIUM).** `settleFXTransaction()` (`index.html:4600`) returns early; no rate-entry modal. Backend `POST /api/fx-transactions/:id/settle` (`server.js:3940`) works. Complements F3 — realised settlement unreachable.
- **PL#10 — Credit notes / vendor credits not applied as contra; recurring profiles inert (🟡 MEDIUM).** Credit notes aren't contra-revenue linked to source invoices (`finflow-bundle.js:3007`); vendor credits aren't contra-AP/expense (`3167`). Recurring invoices/bills are CRUD only, never generate documents on schedule.
- **PL#8 — Items/Inventory are two parallel systems (🟡 MEDIUM).** No `reorder_point` on items (`index.html:3382`, `saveItem` `finflow-api-wiring-medium.js:923`); reorder logic only in Inventory, hardcoded to 10% of max.
- **PL#12 / #13 / #15 — honesty-pass placeholders (🟢 LOW, batch with Step 7).** Banking static "Coming Soon" card (`index.html:1935`); Templates hardcoded array with orphaned `/api/templates` (`app-main.js:4960`); API Connections static catalog whose Connect buttons do nothing.
- **PL#9 — Payroll gross column renders black/invisible (🟢 LOW).** `app-main.js:2230`. Possibly fixed by external commit `2a70564` — **verify**.

**Notes (not findings):** Cash Flow computes locally and inherits the F32 basis (`/api/cashflow` orphaned); Scenario Planner works but can't persist (`/api/scenario` orphaned); MRR works via `/api/recurring-invoices` (`/api/mrr` orphaned dead code).

**ALREADY RESOLVED — do not re-open:** PL#7 (team members fabricated from payroll) → fixed by **F19**; "no real access control" → fixed by **F5 Step 4**; "KPI math duplicated in three files" → fixed by **F7/F24**; fabricated-`$0`-on-failure → fixed by **F31**. (`PRE_LAUNCH_FIX_PLAN.md` predates the reconciliation cluster, so those sections are stale.)

---

## Tracked refinements / env-gated (not open bugs)

Distinct from the open findings above — these are known follow-ups, not defects to fix now:

- **F13 hook hardening** — the pre-commit hook should **warn or refuse** when regenerating the bundle with **dirty unstaged wiring sources** (this is what let `2a70564` bake F31's WIP `medium.js` edit into an unrelated commit's bundle). See the F13 row.
- **F18 follow-ups** (need the live AI key) — (1) the prompt-caching path on `/api/ai` is currently a **no-op** (blocks below the cacheable minimum, breakpoint after `history`); (2) the **Haiku-vs-Sonnet** quality/cost decision needs a live comparison. See "F18 follow-ups" above.
- **Stripe-gated verification** — F11/F12/F17/F21 + the Step F/G migrations are built to completion but **pending `STRIPE_*` / `RESEND_*` env vars** to confirm live. See the Railway section.

---

**Withdrawn:** chat "C1 — stale served bundle (Critical)." Bundles verified in sync by byte-exact re-minify. Residual risk retained as F13.

---

## Railway re-verification (post-deploy — needs a live instance)

> **Methodology note (learned from F32/F33).** Reconciliation proves agreement, not correctness — and single-point verification proves nothing about the rest of the surface. The report-reconciliation cluster (F7/F9/F14/F15/F24) verified every view returns the same number, but (a) never checked whether the canonical formula itself is right, and (b) was only tested on the Year period. Both gaps produced F32 and F33. Going forward: verify across the surface (every period, every view) and sanity-check the formula itself, not just that views agree.

Items verified in a substitute environment (pg-mem / logic harness) that must be re-confirmed against the deployed Railway Postgres + running Express, because the substitute doesn't cover them.

**Live instance:** `finflow-production-dab1.up.railway.app` (the `8e57`/`0817` domains hardcoded in the repo are stale → F29). **Confirmed live so far:** server-up + Express `/api` JSON 404 (also re-confirms F4 ordering); **F10/Step C** `/register?ref=` → 302 `/app?signup=1&ref=` (plan forwarded); **all Step A–G migrations applied** (3/3 tables, 5/5 columns, 3/3 indexes, per live SQL); **F22 CSRF ✅ FIXED + re-verified live** (`sameSite:'lax'` + content-type/Origin guards; forged→415/403, real origin allowed, authenticated same-origin write persists). Remaining below.

- **F22 — CSRF** — ✅ **FIXED & re-verified live** (commit `e293931`): `sameSite:'lax'` + `/api` content-type gate (415) + Origin gate (403); forged cross-site urlencoded/Origin posts rejected, the real serving origin is allowed, and an authenticated same-origin write persists (positive test). See F22 row. **No longer pending.**
- **F5 / Step A — live Express session + middleware wiring for invite/accept** — the harness drove the handlers' SQL directly; it did **not** exercise the real session establishment on accept, `requireAuth`, or the per-request account-resolver middleware. Confirm: accept logs the member in, and their next `/api` request resolves `req.accountId` to the owner.
- **F5 / Step A — real Postgres `jsonb ||` and `jsonb -` operators** — pg-mem lacks the `jsonb || jsonb` key-merge (emulated in JS for the test) and its `-` key-delete was probed but not run in the live UPDATEs. Confirm on real Postgres that the accept UPDATE `(data - 'invite_token_hash' - 'invite_expires') || jsonb_build_object(...)` merges keys and deletes the token fields as intended (single-use).
- **F16 / Step B — accountant session wiring** — the harness drove the handlers' SQL, not live Express sessions. Confirm on the deployed instance: (a) accountant **register mints no session** (a fresh signup cannot reach the portal); (b) a non-verified **login 403 sets no cookie**; (c) the per-request **`requireAccountant` status re-check** fires on every accountant route (an admin suspend blocks the very next request, not just the next login).
- **F10 / Step C — funnel wiring** — ✅ **redirect confirmed live** (`/register?ref=CODE` → 302 `/app?signup=1&ref=CODE`, plan forwarded). Still to confirm via browser walkthrough: the **signup actually posts `referralCode`** (real DOM) so the `accountant_clients` pending link is created.
- **F21 / Step E — RESEND-gated broadcast delivery (needs `RESEND_API_KEY` + `EMAIL_FROM`)** — the harness proved the recipient-selection SQL and the sent/failed/logged branching, not a live send. Confirm on the deployed instance: with a key, a broadcast **actually delivers** and the returned `{sent,failed,total}` match Resend's dashboard; without a key, the route returns `logged:true` / `sent:0` and the admin sees the honest "logged only" toast (no fake delivery). Also confirm soft-deleted users are excluded from every audience on real data.
- **F17 + F11 / Step D — Stripe-gated (needs `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, price IDs, `APP_URL`)** — built to completion; verify each once keys land:
  - **PaymentIntent creation** — `bill-client` creates a live PaymentIntent (currently returns 503 without keys).
  - **`application_fee_amount` + `on_behalf_of` mechanics** — confirm the accountant actually nets `billed − Stripe fee − tier commission` on a real charge (FinFlow keeps the commission, accountant bears the Stripe fee).
  - **Webhook signature + real event delivery** — `constructEvent` verifies, and `customer.subscription.*` + `payment_intent.succeeded` actually arrive and run.
  - **`sub.metadata.userId` presence** — confirm subscription events carry `metadata.userId` (the checkout must set it) so `subscriptionStatus` writes to the right user.
  - **Real balance-transaction fee reconciliation** — `payment_intent.succeeded` retrieves the charge's `balance_transaction.fee` and recomputes the accountant net → `paid`.
  - **End-to-end money flow** — a live subscription flips `subscriptionStatus='active'` → the monthly cron pays that client → cancellation flips to `canceled` and the next cron stops.
  - **Harness caveats (pg-mem gaps, both standard Postgres):** the payout skipped-count uses `IS DISTINCT FROM 'active'` and the `subscriptionStatus` write uses `jsonb ||` — both emulated in the pg-mem harness; confirm they run natively on the deployed Postgres.
  - **Schema migration** — the new `accountant_earnings` columns (`billed_cents`, `commission_cents`, `stripe_fee_cents`, `payment_intent_id`) land via `ALTER TABLE ADD COLUMN IF NOT EXISTS` in `initDB()` on deploy; confirm they apply on the live DB.
- **Step F / Step G — schema migrations that only land on boot (a silent failure is a landmine).** Confirm each applied to the live Postgres after the deploy:
  - **`accountant_documents` table** (Step F) — `CREATE TABLE IF NOT EXISTS` + `idx_acc_docs_accountant` in `initDB()`; without it, credential upload throws on insert and the admin review modal can't list/download. Also confirm `/api/accountants/register` is in `LARGE_PAYLOAD_PATHS` so the base64 body isn't rejected as too large.
  - **`accountants.confirmed_credentials` column** (Step G) — `ALTER TABLE ADD COLUMN IF NOT EXISTS`; without it the approve UPDATE and the `/directory` SELECT both reference a missing column → 500 (approve breaks, marketplace fail-softs to empty). Verify the column exists and the approve→directory round-trip works live.
  - **`ai_usage.scan_count` column + `accountant_ai_usage` table** (F18) — `ALTER TABLE ai_usage ADD COLUMN IF NOT EXISTS scan_count` + `CREATE TABLE IF NOT EXISTS accountant_ai_usage`. Without them the cap read throws → **fail-closed blocks ALL AI** (503) rather than failing open, so a missed migration disables AI entirely (safe but visible). Confirm both exist on the live DB.
- **F18 / AI caps — live smoke test (needs `ANTHROPIC_API_KEY`, now set in Railway).** Harness proved the cap math, not a real Anthropic round-trip. Once the key is live confirm: (a) a normal chat/scan/insights call **succeeds** and returns Haiku/Sonnet output (Haiku quality acceptable on the shared routes); (b) usage **increments** in `ai_usage` / `accountant_ai_usage`; (c) a user driven to their cap gets a clean **402 `AI_CAP_REACHED`** and **no** Anthropic call fires; (d) a cache hit does **not** consume budget.

### F18 follow-ups (tracked — not yet done)
- **(1) Make Anthropic prompt caching on `/api/ai` a real saver.** The route *already* sends `anthropic-beta: prompt-caching-2024-07-31` and marks both the system instruction and the books-context block `cache_control: ephemeral` — but it's currently a **no-op**: both blocks are far below Anthropic's minimum cacheable size (~1024 tokens Sonnet / 2048 Haiku), and the context breakpoint sits **after** the variable `history`, so no stable ≥-minimum prefix ever forms → nothing is ever cached. To actually bill the recurring context at ~10%: (a) enlarge the per-user context to cross the token threshold, and (b) reorder it **ahead of** `history` as the cached prefix (ephemeral TTL ~5 min → benefits same-session follow-ups). Needs a product call on enlarging context (bigger first/uncached prompt is the tradeoff). *Held for decision.*
- **(2) Haiku-vs-Sonnet quality decision (the ~3× cost lever).** Can't be judged without the live key. Once F18 deploys and AI is up, run a controlled **live** comparison — Haiku vs Sonnet on a real receipt scan (`/api/ai/scan`) and a complex finance question (`/api/ai`) — and report outputs side by side to decide whether to default scan+chat to Haiku. **Sonnet stays the default until decided, bounded by the caps.** Needs a live authenticated session (test creds) or the user to drive it.

---

## Confirmed non-issues (checked, not defects)
SQL injection (allow-listed table/field names, parameterized values, `ILIKE … ESCAPE`); Stripe webhook signature (`constructEvent` + secret); admin auth (`timingSafeEqual` + 5/15min limit); password reset (32-byte token, 1h expiry, single-use); bundle sync (byte-exact); landing-page `$469K` hero (marketing mockup).

---

## Recommended fix order

1. ~~**F1** — active cross-tenant data breach. Ship first.~~ ✅ **FIXED.**
2. ~~**F2** — silent data corruption on every partial edit.~~ ✅ **FIXED** (+ payments_made, same class).
3. ~~**F4** — cheap; unblocks correct JSON errors for 42 endpoints (do before/with F14/F15).~~ ✅ **FIXED.**
4. ~~**F3, F6, F8**~~ ✅ **ALL FIXED** — money is wrong on screen (FX P/L, COGS/gross profit, payroll tax). (**F3 ✅ unrealised FX P/L now computed at read time, null when no rate. F6 ✅ standardized on FIFO everywhere, no-cost-basis flagged. F8 ✅ resolved by removing the tax engine.**)
5. ~~**F7, F9, F14, F15, F24**~~ ✅ **ALL FIXED** — reconcile revenue/expense/report into one entity-scoped source. Delivered as a shared canonical basis (`computeBooks` server-side + `computeRevenue`/`computeExpenseBreakdown` client-side, one definition) across dashboard, AI, health, `/api/reports`, the report routes, `/books`, the accountant portal, and the consolidated P&L. Dashboard net == AI == health == `/api/reports` == `/books`; the last-writer flicker is gone; reports entity-scoped + chronological; cash-flow stays cash-basis.
6. **F10, F11, F5, F16, F17, F12, F21, F27** — repair or remove the accountant/RBAC/broadcast funnel end-to-end (interdependent; decide whether the marketplace ships at all before fixing piecemeal). *(F9 ✅ fixed: accountant /books + portal now reconcile with the client dashboard.)*
7. **F13, F18, F22, F19, F20, F23** — hardening: build pipeline, AI cap, CSRF (verify live), DB TLS, dead-code/error hygiene, banking field names.

---

*Two independent audits reconciled. F1–F20 from the runtime-verified code audit; F21/F22/F23 added from the chat audit; the chat "stale bundle" Critical withdrawn as a false positive. F22 (CSRF) — the one item neither pass had settled — is now ✅ fixed and re-verified against the live instance.*

---

## Fixes applied 2026-07-19 — mechanical sweep (agent; F37, F39, F26, F23, PL#6)

Low-risk batch fixed this session. All changed files `node --check`-clean; bundle regenerated + drift-checked (`node bundle.js --check` OK); index.html parse + FX-settle DOM/globals verified in-browser (no console errors). **Not yet committed / not yet live-verified against the server.**

- **F37 — ✅ FIXED.** Added `toLocalYMD(d)` + `todayLocal()` helpers at the top of `public/app-main.js` and replaced the 8 UTC "today" generation sites (`new Date().toISOString().slice(0,10)`) — journal-entry date, personal-tx quick-add (income/expense), tx-modal default, banking quick-add, portfolio snapshot "today", audit-CSV filename, and the `_txNextRun` today-fallback. The one remaining `d.toISOString().slice(0,10)` (line ~3130) is the computed-recurrence formatter, left UTC **intentionally** to mirror the server's `nextRunDate`.
- **F39 — ✅ FIXED (invoices, end-to-end).** Server: `POST`/`PUT /api/recurring-invoices` now accept `end_date`; the scheduler's invoice loop got the same end-date stop-guard the bills loop already had (`server.js` ~2961). UI: added an `#ri-end` "End Date (optional)" input to the recurring-invoice modal and wired both `saveRecurringInvoice` paths (`pages.js` = runtime winner, `stubs.js` = fallback incl. its edit/PUT path) to send it + reset/populate it.
- **F26 — ✅ FIXED.** `sales_receipts` and `payments_received` inserts now set `entity_id: req.entityId || null` (consistent with the invoice/banking POSTs). `findRecentDuplicate` scoping left as-is (null) to avoid changing dedupe behavior.
- **F23 — ✅ FIXED.** Banking POST now writes canonical `tx_type`/`tx_date` (was `type`/`date`) so it matches the rest of `personal_transactions`; the banking GET sort and the client `loadBankingFromDB` mapper read `tx_date||date` / `tx_type||type` so **legacy rows still sort + display**. The request body contract (`{desc,amount,type,date,cat}`) is unchanged — only the stored field names moved. **Also fixed a latent bug found here** (see F42).
- **PL#6 — ✅ FIXED (structurally present, NOT yet exercised).** Built an `#fx-settle-modal` (mirrors `fx-tx-modal`) to collect the settlement rate; rewrote `settleFXTransaction(id)` to open it and added `saveFXSettlement()` to POST `/api/fx-transactions/:id/settle`. Removed the dead `return`/unreachable block (the `prompt()` had been stripped per an earlier audit, leaving the action a no-op). **Caveat:** the fix is verified only at the DOM/globals level (modal opens, no console errors) — the settle round-trip (open position → enter rate → POST → realised gain/loss persisted → position flips to `settled` → reflected in the `/api/reports` FX legs at `server.js:3181-3193`) has **not** been exercised end-to-end. Treat as structurally-present-but-unexercised until a live settle is driven.

### New findings discovered during the sweep

| **F41** | 🟢 **Low — OPEN (new; 2026-07-19)** | **Recurring BILLS support `end_date` server-side but the modal has no field to set it — the stop-condition is dormant.** The `recurring-bill-modal` (`index.html:6391`) exposes only vendor/amount/freq/next/status — no `rb-end` input — so although `runRecurringScheduler` and the bills POST honor `end_date`, a user can never set one. Exact parallel to the invoice half just fixed in F39; not fixed here to avoid scope-creeping the invoice-scoped finding into bills. | `index.html:6391` (modal, no `rb-end`); recurring-bill save wiring | Add an `rb-end` date input + send `end_date` from the recurring-bill save fn (mirror the F39 invoice fix). | **agent add (F39 sweep)** |

| **F42** | 🟢 **Low — ✅ FIXED (new; 2026-07-19; fixed within F23)** | **Banking MTD inflow/outflow KPIs always computed $0.** `renderBanking` filtered `bankTxns` with `(t.date||'').startsWith('YYYY-MM')`, but `t.date` had already been reformatted to a localized display string (e.g. "Jul 19") in `loadBankingFromDB` — so the `YYYY-MM` prefix never matched and MTD inflow/outflow were perpetually $0. | `public/app-main.js` `loadBankingFromDB` + `renderBanking` | **Done:** the mapper now carries a raw `_iso` (`tx_date||date`) alongside the display `date`, and the MTD filter keys on `_iso.startsWith(_ym)`. | **agent add + fix (F23 sweep)** |

| **F43** | 🟢 **Low — OPEN (new; 2026-07-19; NOT fixed — held for decision)** | **EOL normalization is non-deterministic across clones — `core.autocrlf=true` is environment-local, not versioned, and the repo has no `.gitattributes`.** Whether a checkout materializes CRLF or LF working-tree line endings depends on each machine's *global* git config (`core.autocrlf`), which is not tracked in `.git/config` and does not travel with the repo. On a machine with `autocrlf=true` the whole tree is CRLF in the working copy (normalized to LF on commit/diff); on a machine without it, files stay whatever bytes are checked out. Two developers/CI runners can therefore see different raw line endings for identical committed blobs — the exact source of the phantom `postgres.js` 389/389 diff that one environment saw and another didn't during this session. **Related to F13:** the bundle-sync guard (`node bundle.js --check`) only survives this because it explicitly LF-normalizes both sides before comparing (`bundle.js` `norm`); without that defensive normalization, a CRLF/LF checkout difference on any of the 10 wiring sources would trip the drift guard (and the pre-commit auto-regen) on some machines but not others. So bundle determinism currently leans on an ad-hoc workaround for a repo-wide gap. Surfaces on: CI, the Railway build, and any fresh clone on a differently-configured machine. | repo root (no `.gitattributes`); `.git/config` (no versioned `core.autocrlf`); interacts with `bundle.js` `norm` / `.githooks/pre-commit` (F13) | Add a `.gitattributes` pinning line endings so normalization is versioned, not per-machine: `* text=auto` (let git manage EOL normalization on commit) + `*.js text eol=lf` (force LF in the working tree for the sources the bundle concatenates, so the F13 guard is deterministic everywhere). Optionally `*.html text eol=lf` for the inline-script files. Then `git add --renormalize .` once to bake it in. **Not landed here — held for owner's decision on when to renormalize** (a renormalize touches every file's stored EOL, so it wants its own isolated commit, not a rider on feature work). | **agent add (Task A EOL investigation)** |

| **F44** | 🟢 **Low — OPEN (new; 2026-07-19; NOT fixed)** | **Scenario-planner BASE uses the pre-F32 revenue basis.** `_syncScenarioBase` (`public/finflow-api-wiring-medium.js:980-987`) computes `annualRev` from **paid-only** invoices (`status==='paid'`) — the exact pre-F32 recognition F32 Stage 1 replaced with issue-based accrual on every other surface — plus `annualExp` = **all-time** expenses (no period window, no payments_made, no payroll, no bills). It writes `window.BASE = {rev, exp, cash:0, burn}`, which seeds the scenario/what-if planner. So the planner's baseline silently disagrees with the dashboard, `/api/reports`, and `/books` (which are all accrual, F32/F33). An old-basis survivor of the same class as the deleted app-main monthly builder — found during the F38 enumeration, outside its P&L scope. | `public/finflow-api-wiring-medium.js:980-987` (`_syncScenarioBase`) | Rebuild BASE from the canonical engines — `computeRevenue()` / `computeExpenseBreakdown()` (or `computeBooks`) — so the planner baseline matches every other surface. Decide whether BASE should be all-time or a fiscal-year window (align with F25). | **agent add (F38 enumeration)** |

| **F45** | 🟢 **Low — OPEN (new; 2026-07-19; NOT fixed)** | **Budget "actuals" aggregate all-time, with no period window.** The budget-vs-actual builder (`public/finflow-api-wiring-medium.js:1104-1120`) sums `catActuals[cat] += amount` over **every** expense row with no date filter — so "actual spend" for a category is lifetime, not the budget period (month/quarter/year). Compared against a periodic budget target, the variance is meaningless as the dataset ages (actuals only ever grow). Same missing-period-window class as F33, on the budget page. | `public/finflow-api-wiring-medium.js:1104-1120` (budget actuals aggregation) | Filter expenses to the selected budget period via the canonical `_periodWindow` (F33/F25) before aggregating per category, so actuals and targets share one window. | **agent add (F38 enumeration)** |

| **F46** | 🟠 **Medium — OPEN (new; 2026-07-19; NOT fixed)** | **Banking transaction type has no server-side validation, and the client silently defaults unknown types to outflow — money misclassified with no error.** `POST /api/banking` (`server.js:3045-3047`) stores `tx_type: type || 'debit'` with **no allowlist** — any string the body carries is persisted. The client mapper then does `type: (r.tx_type || r.type)==='credit' ? 'credit' : 'debit'` (`app-main.js:4584`) and `amount: Math.abs(r.amount)` (4583), so **anything that isn't exactly `'credit'`** — a typo, a legacy `'Credit'`, an imported `'income'`, a malformed value — is classified as a **debit (outflow)**, coloured red, and subtracted, with **no error surfaced**. A credit silently booked as a debit is the same "wrong money on screen, no signal" class as F31 (fabricated `$0`) and F32 (double-count). Sign on `amount` can't rescue it — `Math.abs` strips it. | `server.js:3045-3047` (`POST /api/banking`, no type allowlist); `public/app-main.js:4584` (silent `else → debit`) | Server: validate `tx_type ∈ {credit, debit}` (reject/400 or normalize explicitly) on POST. Client: map only known values and **surface** an unknown type (flag/"—") instead of silently defaulting to outflow — never classify money by fallthrough. | **agent add (F38 enumeration)** |

| **F47** | 🟢 **Low — OPEN (new; 2026-07-19; NOT fixed; pre-existing, surfaced by Step 2)** | **Cash-flow route dates a paid invoice by `created_at`, so the same invoice is now dated two different ways.** `POST /api/reports/cash-flow` keys paid-invoice inflow on `created_at \|\| due_date \|\| date` (`server.js:3313`). Correctly **untouched** by the F36/Step-2 issue_date work because `/api/reports/cash-flow` is the **cash-basis** statement (decision #2, out of scope). But two things now hold: (1) **the same invoice is dated by `issue_date` in the accrual revenue engines and by `created_at` in cash-flow** — a divergence that is benign today (current invoices have no issue_date → both fall back to created_at) but becomes visible the moment an invoice is issued with an issue_date ≠ its insert date; and (2) on principle a **cash-basis inflow should key on the payment date, not invoice creation** — cash arrives when it's collected, not when the invoice was raised. Point (2) is the same class as **F40** (cash-flow keying on due_date) and lands in the same cluster as the Step-3 settlement work: once `invoice_payments` carries the real payment date, cash inflow should key on **that**. | `server.js:3313` (`/api/reports/cash-flow` paid-invoice inflow key) | Leave cash basis as-is through Steps 2–4; when the F35/settlement work lands, key paid-invoice cash inflow on the **`invoice_payments` payment date** (closing F40 + F47 together). Until then, document that cash-flow's invoice dating intentionally differs from the accrual engines'. | **agent add (Step 2 — F36)** |

| **F48** | 🔴 **HIGH — cross-tenant + correctness on the Store B path (found post-Step-5; fix proposed/built, pending approval)** | **Three defects on `/api/invoice-payments`, all latent until F35/Step 5 made the route UI-reachable, two of them cross-tenant (F1 class).** **(#1a — cross-tenant AR corruption, live-confirmed):** `recalcInvoiceStatus` summed `invoice_payments WHERE invoice_id = $1` with **no `user_id`** (`server.js:~3492`); combined with **(#2)** `POST /api/invoice-payments` doing **no ownership check** (`invoice_id: 999999` → **201** live; a foreign invoice_id → 201), user B can inject a payment row against user A's invoice id, and A's next recalc folds B's amount into A's `amount_paid`/AR/status. **(#1b — delete-revert, live-reproduced 100%):** `recalcInvoiceStatus` status rule `paid>0?'partial':prior` reverted a fully-paid-then-emptied invoice to the **stale current status** — deleting all payments left it stuck `partial` with `amount_paid 0`, AR incoherent (paid → +2000 → +3000 → delete both → stuck partial, AR 0→5000). **(#3 — no amount validation):** `!amount` let `amount:-500` through → **201** live, inflating AR above the invoice total; non-finite/`"abc"` also passed. | `server.js` `recalcInvoiceStatus` (sum scope + status revert), `POST /api/invoice-payments` (ownership + amount + overpayment), computeBooks AR floor; mirror in `recalcBillStatus` | **Fix (server.js only):** scope the sum `AND user_id = $2`; revert payment-derived status to the natural unpaid state when `paid==0` (invoices→`pending`, bills→`unpaid`); `ownedBy('invoices', …)` → 404 on foreign/missing; reject non-finite/≤0 amount → 400; **reject overpayment** (> remaining balance) since there's no credit/refund model; AR = `Σ max(0, amount − amount_paid)` (floor, mirror of the Step 4 AP amendment). **⚠️ ACTIVE KNOWN-GAP — AR is status-driven while AP is arithmetic (asymmetry re-opened on the receivables leg).** After this fix AR still filters `status!=='paid'` whereas AP (Step 4 amendment) is `Σ max(0, amount − amount_paid)` with no status filter. Consequence: a **bare-'paid' invoice that still carries a balance is INVISIBLE to AR** — exactly the defect the AP amendment closed on the payables side, now standing open on receivables. It does not bite today only because `markInvoicePaid` (`medium.js:150`) is a bare `PUT {status:'paid'}` and the 3 live invoices are `paid` with `amount_paid 0`, so flipping AR to arithmetic now would resurrect them as **$19K of AR**. **Fix path (single unit):** (1) `markInvoicePaid` → record a real `invoice_payment` (mirror `markBillPaid`); (2) backfill `amount_paid` on the 3 existing status-only-'paid' invoices; (3) flip AR to `Σ max(0, amount − amount_paid)` (drop the status filter) so receivables matches payables. **No-op on current data (0 invoice_payments), safe to deploy** — but the three sub-steps must land together or AR mis-states. Tracked here until built. **✅ FOLLOW-UP BUILT (2026-07-21, commit `<pending>`), one atomic commit, four sub-steps:** (1) `amount_paid` threaded into both invoice client maps (`app-main.js:1319`, `finflow-api.js:87`) so `markInvoicePaid` can settle only the REMAINING balance — else a partial invoice would POST the full amount and hit the F48 overpayment guard (400); (2) `markInvoicePaid` (`medium.js`) now mirrors `markBillPaid` — POSTs `/api/invoice-payments {amount: remaining, payment_date}` when remaining>0, else a status flip; (3) **boot migration** in `database.js` `initDB` backfills status-only-'paid' rows — **NOTE: the approved typed-column SQL (`ALTER COLUMN amount_paid SET DEFAULT`, `UPDATE … SET amount_paid = amount`) would have thrown "column does not exist" and aborted initDB, because `invoices` is a generic JSONB table (amount/amount_paid/status live in `data`); adapted to a NULL-safe `jsonb_set` guarded by `jsonb_typeof(data->'amount')='number'`** so paid rows with missing/0/NULL `amount_paid` are all backfilled and none stay invisible; (4) AR flip (`server.js:3956`) drops the `status!=='paid'` filter → `Σ max(0, amount − amount_paid)` over ALL recognized invoices. **Verified (pg-mem, jsonb funcs registered to run the real migration SQL unchanged, 7/7):** flip-without-backfill resurrects exactly $19K (proves atomicity); backfill zeroes paid rows incl. missing/NULL `amount_paid`; partial AR = remaining; over-credit floored to 0; migration idempotent; `markInvoicePaid` on a partial pays remaining→201 (full would→400); reconcile arithmetic AR ($8K) == old filtered AR on correct data. Bundle regenerated + drift-checked. **Separate observation (NOT fixed, out of P0-a scope):** the client dashboard KPI cards (`d-outstanding`, `finflow-api-wiring-dashboard.js:187`, `app-main.js:1923/2006`) compute a simpler `Σ amount` over non-paid — they ignore `amount_paid`, so they diverge from canonical AR on PARTIAL invoices; pre-existing display inconsistency, candidate follow-up. | **agent add (post-Step-5 Store B audit; follow-up built 2026-07-21)** |

---

## Live end-to-end health pass — 20 Jul 2026 (dab2, authenticated as owner id 1)

Broad sweep across all 47 nav pages + 29 data endpoints on the live instance. **Overall: structurally healthy — all 47 page containers render (no missing/blank pages), and 27 of 29 data endpoints return 200 with real data.** The accounting core (invoices, expenses, bills, payments, payroll, reports, investments, entities) is intact and serving. Issues below are cleanup/honesty-pass, not core-money defects.

**API probe (200 unless noted):** invoices[3] expenses[1] bills[0] vendors[0] customers[1] quotes[1] sales-receipts[1] payments-received[1] payments-made[0] credit-notes[1] vendor-credits[0] payroll[2] inventory[1] items[1] projects[0] tax-filing{6} holdings[2] entities[1] team[1] audit-trail[6] journals[0] chart-of-accounts[0] templates[0] mrr[1] budget{0} documents[0] recurring-bills[0] fx[0]. **404s:** `/api/portal` (confirms PL#14 — Client Portal has no backend), `/api/timesheets` (page renders; likely route-name mismatch, low stakes — verify).

| # | Sev | Finding | file:line | Fix | Source |
|---|-----|---------|-----------|-----|--------|
| **F49** | ✅ **CORRECTED — NOT A FINFLOW BUG (traced 20 Jul; my earlier "shipped dev script" call was WRONG)** | **`reload.js` is browser-extension-injected, not shipped by FinFlow.** My initial health-pass entry claimed a `reload.js` dev script ships in prod, based on a console screenshot + a live-DOM check that **cannot distinguish extension injection from shipped code**. Definitive re-check: the **raw server-served `/app` HTML contains NO `reload.js` and NO WebSocket** — it ships exactly two scripts (`/app-main.js`, `/finflow-bundle.js`). Code independently confirmed: no `reload.js` file in the repo, no `<script>` include in any HTML, no live-reload/WS middleware in `server.js` (only `express.static`), empty `git log -- '*reload*'`, no livereload dep in `package.json`. The failed `wss://…/ws/ws` in the console is a **local browser live-reload extension** on the machine the console was captured on — FinFlow uses no WebSocket. **The long-standing "reload.js dev script in production" note in prior handovers was itself a carried-forward misdiagnosis — retire it.** | server-served HTML ships only `/app-main.js` + `/finflow-bundle.js` (verified) | **No FinFlow fix required** (disable the browser extension to silence the console error). *Optional* cosmetic hardening: drop the dev-era `ws: wss:` from the CSP `connect-src` (`server.js:75`) since FinFlow uses no WebSocket — turns any stray socket into a documented CSP block. Symptom-level, not a bug fix. | **agent add (health pass) → CORRECTED (verify-before-assert)** |
| **F50** | 🟠 **Medium — OPEN (live-confirmed 20 Jul); = PL#2 boot race** | **`loadEntityData` double-fires on boot** — two identical `[loadEntityData] Restored owner payroll …` logs per load (console-confirmed), and the guard logs `[Entity] loadEntityData already running, skipping`. Part of the PL#2 boot race: KPIs render `$0` until a manual refresh (owner-observed live — signed-in dashboard showed all-$0, correct data only appeared after refresh), and loaders (`Vendors`/`Bills` "loaded 0") fire repeatedly. Root theme: 4 uncoordinated boot paths / loaders firing 6–8×. | `public/app-main.js` boot/`loadEntityData` (`~1276`,`~1293`,`~1377`); the 4 boot paths (PL#2) | Single coordinated boot: one load pass, dedupe `loadEntityData`/`loadPersonalFinance`, render KPIs after data resolves (not before). Fixing this also removes the first-load `$0` flash. | **agent add (health pass); = PL#2** |
| **F51** | 🟡 **Low — OPEN (live-confirmed 20 Jul)** | **Placeholder pages presented as live features.** Live-confirmed "Coming Soon" / no-backend surfaces: **Banking** ("Coming Soon", = PL#12), **Find Advisor** ("Coming Soon", a `NEW`-badged nav item), **Client Portal** (`/api/portal` → 404, = PL#14), **Templates** (empty, no persistence, = PL#13), **API Connections** (static ~98 KB catalog, "Connect" does nothing, = PL#15). Also flagged for review: the **tax-filing** container carries a "Coming Soon" fragment *while* `calcAndRenderTax` still fabricates figures (PL#11) — contradictory, needs eyes; and the **team** container carries a "coming soon" fragment though `/api/team` returns real data (likely a sub-section, verify). | per PL#12/#13/#14/#15; `tax-filing` + `team` containers | Honesty pass: hide/disable un-built features or label them clearly "coming soon" without fake controls; resolve the tax-filing + team placeholder fragments. | **agent add (health pass); rolls up PL#12–#15** |
| **F52** | 🟡 **Low — OPEN (live-confirmed 20 Jul; a11y)** | **Form-accessibility gaps** (DevTools Issues, owner-observed): **3 form fields with no `id`/`name`** and **7 inputs with no associated `<label>`**. Prevents correct autofill and breaks screen-reader labelling. | form fields across modals/pages (3 no-id/name + 7 no-label — enumerate via DevTools "Violating node" links) | Add `id`/`name` + associated `<label for=…>` (or nest input in label) on the flagged fields. | **agent add (health pass)** |

**F50 live boot-trace (20 Jul, dab2, single fresh load — completes the static map):** **152 console messages on ONE load.** The boot stack runs 2–4× over: **`loadEntityData` fires ≥2×** (console msg 18 "Loading data for entity_id=1" then again msg 23), each **zeroing REV/EXP at the top of the run then refilling at the end** (`app-main.js:1280-1283` → `1416`); **the entire wiring stack re-initializes ≥2×** (the "All easy patches / Medium / Stubs / Pages / Extra / Postgres wiring active" banners appear at msgs 1–6 AND again 32–37); **per-page loaders fire 3–4× each** (`Vendors loaded 0` at msgs 9, 24, 30, 40…). **Nondeterminism confirmed:** on THIS load the last `loadEntityData` fill won → KPI settled correct ($19.0K/$50.0K/−$31.0K) with NO refresh; on the owner's earlier screenshot a render landed in a zero-window and nothing corrected it → `$0` until refresh. Same code, different async-resolution order = race (this is the live-trace piece the static map flagged as open — now closed). **Bonus finding:** even on a "good" KPI load, `window.REV` was still `[0×12]` while the KPI read $19.0K — the KPI (via `computeRevenue`) and the monthly array `REV[]` (the overview chart's source) are filled by **different** paths, so the **overview chart can render empty while the KPI card is correct**. Root cause to pin for the fix: *why the whole wiring stack + loaders re-execute* (the banners re-appearing implies the bundle IIFEs or loader-binding re-run — likely an entity-activate/re-render retrigger). Fix must be a single coordinated boot (one auth, one load pass, render-after-resolve), not a per-loader guard.

**⚠️ CORRECTION — the QA "AI leaks `ANTHROPIC_API_KEY`" item is NOT a secret leak.** Live-checked: `POST /api/ai` returns `502 {"error":"AI service unavailable. Add ANTHROPIC_API_KEY to .env to enable."}` — the response contains the env-var **name** as guidance, **no `sk-ant-…` value is exposed** (regex-checked, key redacted in the probe). Downgrade from 🔴 secret-leak → 🟡 **misleading error**: (a) it says the key is *missing* when it is actually *set-but-uncredited* on Railway (the app maps any Anthropic failure to "add key to .env"), and (b) it returns **502** rather than a clean 402/503. Fix: distinguish "not configured" from "configured but the upstream call failed," and don't tell a user to edit `.env` on a hosted deploy. (Verifying before acting caught this — same discipline as the earlier expense false-alarm.)
