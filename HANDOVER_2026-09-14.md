# FinFlow — Session Handover (2026-09-14)

_For the next Claude (code account). Everything below is verified against code + harnesses this session._
_Repo: theking012012-jpg/FinFlow. Owner commits in PowerShell (assistant can't commit — OneDrive `.git/index.lock`)._

## TL;DR

A heavy **security + ops hardening** run. Everything shipped is pushed to `main` and the full sweep is
**green (238 harnesses; targeted 29/29 confirmed after all shared-code edits)**. Nothing is half-done.
The remaining work is the deliberately-deferred big items (RLS build, perf min-build, FX consolidation)
— all now **designed** so they're fast to build next.

## Shipped + pushed this session (all harnessed, all green)

**Auth / MFA**
- Owner-account TOTP MFA — login gate + `/api/auth/mfa/{status,setup,enable,disable}`, secret AES-GCM
  at rest in `users.data`, login-page code field + Settings 2FA enrollment card (`finflow-owner-mfa.js`).
  Harnesses: `verify-owner-mfa` 18/0, `verify-owner-mfa-login-ui` 13/0, `verify-owner-mfa-enroll-ui` 26/0.
  (Accountant MFA was already shipped; `verify-accountant-mfa` 17/0.)

**Rate limiting**
- Cloudflare-aware rate-limit key: `authLimiter`/`acceptLimiter` + `_rlKey` fallback key on the real
  client via `_clientIp` → `CF-Connecting-IP` → Envoy/XFF → req.ip. `verify-cloudflare-rl-key` 6/0.
  ⚠ Spoof-proof ONLY once the Railway origin is locked to Cloudflare (INFRA_RUNBOOK step 7).

**Audit-anomaly system (detect → deliver → auto-scan), all in `audit-anomalies.js`**
- Detector `detectAuditAnomalies` + admin `GET /api/admin/audit-anomalies`. Signals (6): accountant
  cross-client, activity spike, mass delete, login_multi_ip, mass_export, failed_login_burst
  (brute-force). Env-tunable thresholds (`AUDIT_ANOMALY_*`).
- Login + document-download + failed-login events now audited (LOGIN / EXPORT / LOGIN_FAILED rows).
- Email delivery `notifyAnomalies` + `POST /api/admin/audit-anomalies/notify` (Resend).
- Auto-scan `startAnomalyMonitor` on boot (guarded: no-op unless `SECURITY_ALERT_EMAIL` set; unref'd).
- Harnesses: `verify-audit-anomalies` 10/0, `verify-audit-anomaly-notify` 11/0, `verify-anomaly-events`
  9/0, `verify-anomaly-failed-login` 9/0, `verify-anomaly-monitor` 7/0.

**Ops / posture**
- Public `GET /healthz` — DB-reachability, 200 healthy / 503 degraded, no-store. `verify-healthz` 6/0.
- KYC Phase C — `kyc-registry.js` maps a professional body to its OFFICIAL register (ACCA/ICAEW/ICAS/
  CIMA/US-CPA-NASBA/CPA-Ontario/CPA-Alberta; URLs verified) with web-search fallback; wired into admin
  modal. `verify-kyc-registry` 14/0.

**Regression guards locked in (existing behavior, now permanent gates)**
- Connector-token encryption at rest (all 13 connectors) — `verify-connector-token-encryption` 11/0.
- Tenant isolation / IDOR — `verify-tenant-isolation` 47/0 (also caught + fixed a real cross-tenant
  bill-delete fake-success earlier).
- Audit-trail append-only immutability — `verify-audit-immutability`.
- HTTP security headers (CSP/frameguard/nosniff/referrer/HSTS) — `verify-security-headers` 10/0.
- Session cookie flags (HttpOnly/SameSite/prod-Secure) — `verify-session-cookie-flags` 6/0.
- Upload/download XSS hardening (forced attachment + nosniff) — `verify-upload-download-hardening` 7/0.

**Also earlier in these sessions:** pricing overhaul (Pro unlimited + 500 fair-use ceiling, Business
$249, Scale $400/10-entities), 30-day trial everywhere, per-entity + personal accountant access,
bcrypt cost-12, `npm audit fix` (3→2; last one needs express 4→5), region-strict bank routing,
Stripe Identity KYC Phase B.

## Design docs ready to build from (no code yet)
- `RLS_DESIGN.md` — Postgres Row-Level Security. Crux: pooled `pool.query` autocommit path + the
  FORCE-RLS owner gotcha → needs an ALS request-context + transaction wrapper. Accountant dimension
  handled by setting the GUC to the client's user_id. Staged, reversible rollout.
- `FX_CONSOLIDATION_DESIGN.md` — world-class #1. The F34 conversion engine already exists; the gap is
  a base-currency setting + defaulting the CONSOLIDATED view to convert (not native raw-sum). Small.

## Remaining code work (deferred deliberately, not blocked by bugs)
- **RLS build** — the careful staged data-layer pass (design ready). Do with the FULL sweep watched.
- **FX consolidation build** — per `FX_CONSOLIDATION_DESIGN.md` (A base-currency + B consolidated
  default + C coverage banner; D rate feed later).
- **Mobile perf min-build** — `.min.js` via esbuild/terser + index.html/SW refs + a Playwright smoke of
  the BUILT output. Babysat pass (blast radius = the served app).
- **Sentry error monitoring** — needs a DSN (ops). `/healthz` already covers the uptime half.
- **express 4→5** — clears the last `qs` advisory; major bump, own pass.
- **CSP `unsafe-inline` removal (L3)** — 623 inline handlers → addEventListener; large.
- **Impossible-travel (true geo-velocity)** — needs a geo/IP-intel service (login_multi_ip is the proxy).

## OWNER / OPS (you, in dashboards — not code). Full steps in INFRA_RUNBOOK.md + LAUNCH_PUNCHLIST.md
- Stripe: Business price → $249; create Scale $400 price + set `STRIPE_PRICE_SCALE` (Railway).
- Enable Stripe Identity in Stripe.
- Resend: verify `finflow.app` domain (SPF/DKIM) + `EMAIL_FROM` — launch blocker for real-user email.
- Set `SECURITY_ALERT_EMAIL` (turns on anomaly auto-alerts) — pairs with Resend.
- Cloudflare in front + TLS Full(strict) + WAF, and **lock the Railway origin to Cloudflare** (makes
  the CF-Connecting-IP rate-limit fix spoof-proof).
- Provider go-live keys (Plaid prod, Belvo, +8 connectors).
- Tested backup + PITR restore drill; least-privilege DB role (also blocks audit_trail TRUNCATE); secrets rotation.
- **Move the repo out of OneDrive** — ends the recurring `.git/index.lock` fight.
- Prod test-data cleanup (QA Tester + Claude TestCPA rows).

## The done-gate
- Full `VERIFICATION.md` re-sweep on real seeded data before launch sign-off (238 harnesses green now
  on scratch PG; the launch gate is the owner-derived-figures sweep on the canonical seed).

## How to run the gate
```
node tests/harness/run-verification-sweep.js                 # full (238, ~long)
$env:SWEEP_ONLY="auth,mfa,anomaly,security,tenant,invoice"; node tests/harness/run-verification-sweep.js   # targeted
node -r ./tests/harness/clock.js tests/harness/<one>.js      # single (PG-backed ones need the clock preload)
```
New `verify-*.js` are auto-discovered into the sweep — no registration needed. `expected.js` is only for
the hand-derived money figures.
